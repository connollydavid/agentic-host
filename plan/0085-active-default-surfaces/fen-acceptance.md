# fen-acceptance: the pointer and the strict tier (2026-09-05)

Status: **the probes are owed.** The gateway answers 401 to a session with no channel credentials (the plan/0084 record carries the labeled attempt; nothing has changed), so the kit cell is built and preserved, and no ballot was simulated.

[fen-p1.txt](fen-p1.txt) is the pointer probe: handed a repository whose CLAUDE.md is only the pointer line, does the model open AGENTS.md as the manual and name a correct next action? Two draws on the plan/0076 corrected protocol, appended to fen-results.md. Pass = both draws follow the pointer. The reading-safety probe from plan/0084's kit carries over unchanged: the strict verdict must not capture the agent into remediating records.
