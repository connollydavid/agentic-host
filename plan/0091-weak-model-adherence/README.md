# plan/0091 weak-model adherence: make doctrine govern at depth

From plan/0090's null: the repaired section still fails on qwen3.5-4b at 131072 tokens (48/48 role-confusion draws). This milestone measures context-shape remedies, prototypes the output gate, tries the STE register, and trains the edge model. Harness: plan/0090's, carried over.

## Arms

1. **Triage**: radius curve (2k/16k/32k/64k), think-block mining, prefill probe.
2. **Matrix** (max context, 8 draws/cell, weco-logged): tail line, reply-start anchor, few-shot exchanges, STE section, STE tail line. Winners get 16-draw confirmation.
3. **Spine**: survivors become deployment doctrine: the governing radius, the canonical tail line.
4. **Gate**: stdlib scorer + OpenAI-compatible proxy; forced-regeneration demo.
5. **LoRA**: unsloth on the edge 4B; harness as eval; general capability must hold.

## Results

As runs land.

## Standing constraint

Terse writing everywhere: plans, docs, doctrine lines, commits.
