# gather-data: the six-term measurement, the transcripts, the trio, the skill gap (2026-09-06)

This records the #gather-data node. The measurement method is call/0051's: word-boundaried, case-folded occurrence counts over the swept corpora, each occurrence classified.

## The six house-diction terms, measured

Corpus: this host's tracked markdown (records included; the untracked harness plan file excluded), plus the template's markdown, which carries zero hits for all six.

| term | occurrences | genuine prose uses | classification |
|---|---|---|---|
| `realm` | 3 | 0 | every occurrence is call/0051 discussing the term itself |
| `underscore` | 2 | 0 | meta-mentions only |
| `showcase` | 3 | 0 | meta-mentions only, including call/0051's own zero-use ruling |
| `intricate` | 2 | 0 | meta-mentions only |
| `nuanced` | 5 | 3 | the genuine uses sit in plan/0080's `fen-results.md`: the real qwen3.5-4b's raw transcript (`nuanced instructions`, `too nuanced or subjective`, `nuanced best practices`): a record, excluded from the sweep, preserved unedited |
| `multifaceted` | 2 | 0 | meta-mentions only |

**Verdict: zero over-firing evidence.** The rule is inert on this corpus: the terms never appear in genuine prose, so they never fire falsely, and the one genuine-use population lives in an excluded record. Narrowing or retiring on a zero-collision measurement would repeat the uncalibrated move call/0051 refused for `showcase` ("narrowing it on a reading of the word would repeat the uncalibrated move that produced this defect"). The measurement is the evidence base the deferred question asked for, and its answer is: **the terms stay detecting**, with the adopter corpus as the place any collision would surface and the LEXICON report path as the mechanism that surfaces it.

## The held-out trio

`framework` (14 host hits + 1 template), `paradigm` (13 + 2), `ecosystem` (5 + 1) remain dominated by technical uses: the merge-driver framework, the actor paradigm, the package ecosystem. call/0051's hold-out stands on this reading; the re-admission invitation stays unexercised, and the corpus reading is now on the record beside it.

## The two closures' transcripts

Both captured at [verify-transcripts.txt](verify-transcripts.txt):

- **#27**: `host-lifecycle prose host-template` at `prose: clean … [host-lint 0.19.0]`, exit 0: the cited double-fire does not reproduce.
- **#24**: `host-lifecycle --version` answers `host-lifecycle 0.53.0`; the stanza records the toolchain, the canonical artifact hash (`216055a4…`, v0.53.0), and the deps-bundle (vendor-v7); `software --check` exits 0.

## The SKILL.md delta

host-lint's SKILL.md carries exit-code guidance and the adoption-audit section but never mentions `LEXICON` or `host-lint lexicon add`, while the strict directive's block message names `host-lint lexicon add` as its remedy and the README documents the full CRUD with its three abuse guards. The delta to close: a compact LEXICON section in SKILL.md: the mechanism, the remedy command, and the declaration-is-a-report doctrine in one or two sentences.
