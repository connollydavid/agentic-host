# Paths excluded from `host-lint --all` (call/0009): not the host's own authored
# live content, so out of scope for the host's naming audit.

# The software under test — a bare store + worktrees (call/0010), audited in its
# own repo, not here. Local/gitignored; --all walks the filesystem so name them.
host-lint/
host-lint.git/

# The referenced methodology template — audited upstream, not host content.
host-template/

# Generated mdBook output.
book/

# The append-only record (call/0009): acknowledged history, left verbatim. The
# per-milestone working docs (READMEs, design reviews) reference numbered plan
# steps and old milestone names; the curated PLAN.md index and the call/ decisions
# stay linted (call/ caught a real ordinal tell; see call/0019).
MEMORY.md
plan/*/*.md
