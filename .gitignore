# Software-under-test: bare store + worktrees (see .host-software, call/0010).
# Materialized locally from the recipe; never tracked.
/host-lint/
/host-lint.git/
/host-lint.*/
/host/
/host.git/
/host.*/

# Skill symlink into the worktree — local, recreated after materialize. Tracking
# it would dangle in a fresh clone / CI (worktree-absence coherence, call/0011).
/.claude/skills/*

# Generated by `host-lifecycle book` (the canonical publisher, call/0014) and by
# mdBook; never committed, regenerated by CI before the build.
/book.toml
/docs/
/book/
