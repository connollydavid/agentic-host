[receipt "plan/0041#gather-data"]
    disposition = done
    verify = attested operator
    evidence = Qwen-3.5-4B (Fen) run recorded in plan/0041-next-fails-closed/gather-data.md; chosen form: distinct diagnostic + did-you-mean line, non-zero exit (code 2)
    tool = host-lifecycle@0.26.0
    recorded = 2026-06-25

[receipt "plan/0041#implement-fail-closed"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test
    evidence = cargo test: 101 passed (incl. next_fails_closed_on_numberless_dirs); cargo clippy clean; built-binary check: next . exits 2 with a did-you-mean for a real room, next plan unchanged
    tool = host-lifecycle@0.26.0
    recorded = 2026-06-25

[receipt "plan/0041#record-the-call-decision"]
    disposition = done
    verify = host-lifecycle validate call/
    evidence = call/0025-next-fails-closed.md written and accepted; host-lifecycle validate call/ = ok; prose clean
    tool = host-lifecycle@0.26.0
    recorded = 2026-06-25

[receipt "plan/0041#release-and-re-pin"]
    disposition = done
    verify = attested operator
    evidence = host-lifecycle v0.26.1 released (commit 2f606e29, tag v0.26.1); .host-software re-pinned + release receipt recorded; software --check green and --verify-build reproduces artifact adf5c10a offline; 3 CI install pins bumped (7b1ffaa)
    tool = host-lifecycle@0.26.1
    recorded = 2026-06-25

[receipt "plan/0041#close-the-issue"]
    disposition = done
    verify = attested operator
    evidence = connollydavid/host-lifecycle#1 CLOSED (auto-closed by release commit 2f606e29 'closes ...#1'); closing comment added referencing v0.26.1, call/0025, plan/0041
    tool = host-lifecycle@0.26.1
    recorded = 2026-06-25

[receipt "plan/0040#settle-open-decisions"]
    disposition = done
    verify = attested operator
    evidence = Two-lens adversarial review recorded in plan/0040-front-door-from-spine/design-review.md; verdict proceed-with-changes (re-scope to structured-source generation + coverage); four open decisions settled
    tool = host-lifecycle@0.26.1
    recorded = 2026-06-25

[receipt "plan/0040#add-front-door-generator"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test
    evidence = front-door + front-door --check subcommands added (phase coverage from manifest, tool coverage from .host-software drivers + lifecycle engine, byte-exact .host stamp generation); cargo test 102 passed (incl. front_door_covers_phases_tools_and_stamp), clippy clean; live run HAZARDs the omitted release phase
    tool = host-lifecycle@0.26.1
    recorded = 2026-06-25

[receipt "plan/0040#section-and-regenerate"]
    disposition = done
    verify = attested operator
    evidence = Added the release phase to the front-door README (host e73ee98), re-pinned .host-software; front-door --check clean (all phases + tools named, stamp canonical), host prose clean, software --check at pin. Stamp regeneration is a no-op (already canonical); no seed stamp (re-scope).
    tool = host-lifecycle@0.26.1
    recorded = 2026-06-25

[receipt "plan/0040#wire-the-check"]
    disposition = done
    verify = attested operator
    evidence = Added 'host-lifecycle front-door --check .' as a step in reproducible-build.yml (agentic-host CI, where sources are materialized; lands atomically with the v0.27.0 pin in the release commit). Deliberately-staled front door fails the check: stamp drift HAZARDs (exit 1), and a phase named nowhere HAZARDs (the original release omission). Separate CI step, not the spine recheck, so no adopter obligation.
    tool = host-lifecycle@0.26.1
    recorded = 2026-06-25

[receipt "plan/0040#release-and-re-pin"]
    disposition = done
    verify = attested operator
    evidence = host-lifecycle v0.27.0 released (302e87f8, tag v0.27.0, artifact 0f63d960); .host-software re-pinned + release receipt; CI install pins bumped and front-door --check wired into the reproducible-build job; call/0026 recorded; software --check, --verify-build (offline reproduce), and front-door --check all green
    tool = host-lifecycle@0.27.0
    recorded = 2026-06-25

[receipt "plan/0043#settle-open-decisions"]
    disposition = done
    verify = attested operator
    evidence = Three-reviewer adversarial review (design-review.md) returned re-scope; operator re-cut to expand the milestone. Declaration confirmed as a singleton [entrance] stanza reaching any document by path, member set apart; marker-break designed out. Decisions settled: stanza declaration form, restates concept-set against homes, separate-subcommand reconcile sibling, spine reconciliation in milestone.
    tool = host-lifecycle@0.28.1
    recorded = 2026-06-25

[receipt "plan/0043#implement-entrance"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test
    evidence = Singleton [entrance] stanza implemented in host-lifecycle: parse reads [entrance] (member, document default README.md, restates with true sentinel + closed {phases,tools,stamp} vocabulary), member set apart from components, legacy per-member marker shimmed with a deprecation warning naming the stanza, entrance_readme resolves the document path, entrance_problems routes by restates, exit-code convention comment added. cargo test 102 passed (new stanza/singleton/unknown-concept/member tests), clippy clean; legacy entrance=true on agentic-host warns + checks clean.
    tool = host-lifecycle@0.28.1
    recorded = 2026-06-25

[receipt "plan/0043#implement-entrance"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test
    evidence = Singleton [entrance] stanza implemented and adversarially reviewed (code-review.md, three reviewers). Two blocking defects found and fixed: problems were surfaced only to entrance, so a typo'd member reintroduced the call/0027 silent demotion via reconcile (reconcile now surfaces problems, exit 2); an empty restates parsed to check-nothing and reported clean (now a problem, exit 2). Parser holes fixed: sub-named [entrance "x"] flagged, document path-escape rejected, true sentinel case-insensitive, duplicate member flagged. Exit-code framing corrected to issues-found(1)/cannot-proceed(2). cargo test 102 + new problem-path tests, clippy clean; verified on the binary.
    tool = host-lifecycle@0.28.1
    recorded = 2026-06-25

[receipt "plan/0043#spine-doctrine"]
    disposition = done
    verify = attested operator
    evidence = host-template ba86125: the components concept definition sets the entrance member apart with an [entrance] stanza (was front-door = true); added the entrance-check doctrine (reconcile's standalone sibling, the [entrance] stanza form) to CLAUDE.md; added UPGRADING entry 2dbcfd7 (requires host-lifecycle v0.29.0) migrating a legacy per-member marker to the stanza. host-template prose clean (host-lint --docs exit 0).
    tool = host-lifecycle@0.28.1
    recorded = 2026-06-25

[receipt "plan/0043#migrate-agentic-host"]
    disposition = done
    verify = attested operator
    evidence = Migrated .host-software from the per-member entrance = true to the [entrance] stanza (member=host, document=README.md, restates=true); STRUCTURE.md marker reference updated to the stanza; adopted host-template b7cc250 (upgrade --record ba86125 via verify, 0 pending). entrance --check clean via the stanza (no deprecation warning), reconcile clean, software --check at pin.
    tool = host-lifecycle@0.29.0
    recorded = 2026-06-25

[receipt "plan/0043#validate-4b"]
    disposition = done
    verify = attested operator
    evidence = Qwen-3.5-4B via pal (gather-data records the runs): authoring fill-in-the-blank for a skill restating only phases gave member=my-skill, document=SKILL.md, restates=phases (converged before the pal format-loop); comprehension gave the entrance document, the phases and tools concepts, and the .host stamp correctly excluded. Singleton, document path, restates subset, and the closed vocabulary all read correctly.
    tool = host-lifecycle@0.29.0
    recorded = 2026-06-25

[receipt "plan/0043#release-and-re-pin"]
    disposition = done
    verify = attested operator
    evidence = Released host-lifecycle v0.29.0 (dbeb3db, artifact 01171b92); re-pinned .host-software; bumped host-template pointer to b7cc250 and adopted ba86125; CI install pins bumped to dbeb3db; release receipt recorded; call/0028 records the decision (supersedes the scope of call/0026, carries call/0027 to completion). Whole-suite verification follows in CI.
    tool = host-lifecycle@0.29.0
    recorded = 2026-06-25

[receipt "plan/0043#retire-shim"]
    disposition = done
    verify = attested operator
    evidence = Retired the deprecate-then-retire shim (host-lifecycle v0.30.0, 94984a1): a surviving per-member front-door = true / entrance = true is a loud problem in every consumer (parse_project_facts pushes it; entrance and reconcile exit 2), never the entrance. Spine retirement doctrine + adopter UPGRADING entry (host-template de8a517, requires v0.30.0); agentic-host adopted it (upgrade --record de8a517 via verify). The live spine teaches only the stanza. cargo test 102 + retired-marker tests, clippy clean; entrance --check clean on the stanza, a legacy marker exits 2.
    tool = host-lifecycle@0.30.0
    recorded = 2026-06-25

[receipt "plan/0044#settle-by-data"]
    disposition = done
    verify = attested operator
    evidence = gather-data.md: direct-rope Fen run; (A) lane consistency + (ii) surgical word boundary + (b) no new abuse surface
    tool = host-lifecycle@0.30.0
    recorded = 2026-06-25

[receipt "plan/0044#extend-mask"]
    disposition = done
    verify = cd software/host-lint/main && cargo test
    evidence = cargo test green: 73 unit (2 new) + 110 integration (2 new); clippy clean
    tool = host-lifecycle@0.30.0
    recorded = 2026-06-25

[receipt "plan/0044#readme-claim"]
    disposition = done
    verify = attested operator
    evidence = README masked-modes wording adds --prose and --docs; prose gate clean
    tool = host-lifecycle@0.30.0
    recorded = 2026-06-25

[receipt "plan/0044#release-and-close"]
    disposition = done
    verify = attested operator
    evidence = host-lint v0.10.0@5106ee7a (commit f1474e8, tag pushed); .host-software re-pinned; release receipt recorded; host-lint issue #16 closed
    tool = host-lifecycle@0.30.0
    recorded = 2026-06-25

[receipt "plan/0044#release-and-close"]
    disposition = done
    verify = attested operator
    evidence = host-lint v0.10.1@941126c9 (commit 0c2bfc3, tag pushed); feature shipped as v0.10.0 then v0.10.1 re-derived the staled DetectInternalCodeAsName obligation digests; .host-software re-pinned; receipt recorded; host-lint #16 closed
    tool = host-lifecycle@0.30.0
    recorded = 2026-06-25

[receipt "plan/0045#reproduce"]
    disposition = done
    verify = attested operator
    evidence = fixture diverges: host-lint --docs masks the declared phrase (exit 0) while host-lifecycle prose warns (exit 3) on the same repo; root cause is embedded host_lint v0.8.1 whose scan_prose_text takes no allowlist
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0045#share-engine"]
    disposition = done
    verify = cd software/host-lint/main && cargo test
    evidence = host-lint v0.10.2@5a9d2c5: Lexicon/load_lexicon/run_docs public in lib, binary calls them, run_docs returns Result; cargo test green (75 unit incl 2 new + integration), clippy clean; --docs behaviour byte-identical
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0045#consume-engine"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test
    evidence = host-lifecycle host-lint dep bumped to v0.10.2; prose_audit calls the shared loader+walk; cargo test green (103 incl new prose_audit_honours_lexicon_masking); fixture exits 0 with LEXICON, 3 without
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0045#vendor-v2"]
    disposition = done
    verify = attested operator
    evidence = vendor-v2 published on host-lint releases (sha 4e49536b) covering host-lint v0.10.2 source; host-lifecycle deps-bundle re-pinned in .host-software + deps-bundle.lock; offline --network none release build reproduced hash 23c27bff
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0045#release-and-close"]
    disposition = done
    verify = attested operator
    evidence = host-lint v0.10.2 + host-lifecycle v0.30.1 released and tagged; .host-software re-pinned; 5 CI install pins bumped; release receipts recorded; software --check clean; host-lifecycle#2 closed
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0046#decide-layout"]
    disposition = done
    verify = attested operator
    evidence = Fen (real qwen3.5-4b via rope, true system prompt): fair tie-break favoured consolidation once ensemble conventions stated; acceptance test 5/5 docs-safe under mdBook/ layout, 3/3 footgun under old docs/-as-output; five cast personas confirmed + set 4 UPGRADING requirements
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0046#book-generator"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test
    evidence = write_book -> mdBook/src; book_toml src="mdBook/src" + build-dir="mdBook/out"; help/doc-comments/dry-run/error strings + book_toml test renamed; new regression test write_book_targets_mdbook_dir_and_leaves_docs_intact; cargo test 104 pass, clippy -D warnings clean
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0046#spine-and-ledger"]
    disposition = done
    verify = attested operator
    evidence = host-template e068828: STRUCTURE.md doctrine -> mdBook/src + mdBook/out (book.toml at root); .gitignore /book.toml + /mdBook/; UPGRADING entry e068828 (independent, requires host-lifecycle v0.30.2, fail-safe cleanup, verify greps mdBook/src in STRUCTURE.md); pushed cecb0eb; host-template prose CI clean
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0046#repoint-agentic-host"]
    disposition = done
    verify = attested operator
    evidence = agentic-host .gitignore -> /book.toml + /mdBook/ (dropped /docs/ + /book/); removed stale generated docs/ + book/; regenerated with new binary: book . -> book.toml(src=mdBook/src, build-dir=mdBook/out) + mdBook/src/SUMMARY.md + 95 pages, book --check green, local mdbook build -> mdBook/out. mdbook.yml publish_dir -> mdBook/out is applied with the CI rev bump in the release commit (coupled).
    tool = host-lifecycle@0.30.1
    recorded = 2026-06-26

[receipt "plan/0046#release-and-verify"]
    disposition = done
    verify = attested operator
    evidence = host-lifecycle v0.30.2 (f2b4607, artifact 3a9e59ce, vendor-v2) released+tagged; .host-software re-pinned; release receipt recorded; host-template pointer -> cecb0eb; upgrade --record e068828; mdbook.yml publish_dir -> mdBook/out + rev v0.30.2; reproducible-build.yml rev v0.30.2; software --check clean
    tool = host-lifecycle@0.30.2
    recorded = 2026-06-26

[receipt "plan/0047#investigate-decide"]
    disposition = done
    verify = attested operator
    evidence = reproduced: untracked authored .md skipped by git ls-files walk (clean), staged/committed flagged; skip is intentional (excludes generated/vendored/worktrees) and staging closes it; cast (Bly overstates-completeness, Fen silent-trap, Orin fail-unsafe) frames silent-partial-clean as a defect; operator chose fix (a) widen the walk
    tool = host-lifecycle@0.30.2
    recorded = 2026-06-26

[receipt "plan/0047#scan-working-tree"]
    disposition = done
    verify = cd software/host-lint/main && cargo test
    evidence = run_docs walks git ls-files (tracked+staged) + git ls-files --others --exclude-standard (untracked-non-ignored) via a git_paths helper; disjoint sets, no dedup; per-file logic unchanged (.md only, .host-lintignore, symlink skip); new test run_docs_scans_untracked_authored_but_not_gitignored; cargo test 76 pass, clippy -D warnings clean
    tool = host-lifecycle@0.30.2
    recorded = 2026-06-26

[receipt "plan/0047#validate"]
    disposition = done
    verify = attested operator
    evidence = CLI: #17 repro untracked authored.md flagged (exit 3), gitignored gen excluded; agentic-host: git ls-files --others --exclude-standard finds no .md (generated mdBook/+book.toml gitignored), --docs exit 0 -> CI unchanged. Fen (real qwen3.5-4b via rope): new walk catches unstaged doc 5/5, old walk misses 3/3. Cast Bly/Fen/Mara/Orin served; no spine change
    tool = host-lifecycle@0.30.2
    recorded = 2026-06-26

[receipt "plan/0047#release-cascade"]
    disposition = done
    verify = attested operator
    evidence = host-lint v0.10.4 (ce683be, a9ef0865; kani digests re-derived cc31f540 BEFORE tagging, green first try) released+tagged; host-lifecycle host-lint dep -> ce683be; vendor-v3 published (817a71d9); host-lifecycle v0.30.3 (0de2843, cf59cc16) built offline against vendor-v3, released+tagged; .host-software re-pinned; vendor leftovers cleaned
    tool = host-lifecycle@0.30.3
    recorded = 2026-06-26

[receipt "plan/0047#close-and-verify"]
    disposition = done
    verify = attested operator
    evidence = agentic-host CI pins (mdbook.yml + reproducible-build.yml x2) -> v0.30.3; host-template/host prose CI left at v0.30.1 (fresh-checkout behavior unchanged by #17); release receipts recorded; software --check clean; host-lint#17 to close
    tool = host-lifecycle@0.30.3
    recorded = 2026-06-26

[receipt "plan/0048#validate-strictness"]
    disposition = done
    verify = attested operator
    evidence = Fen (real qwen3.5-4b, rope, non-leading after discarding a leading round): flaw-spot on PATH check 6/8 see on-PATH != works (version mismatch, wrong args, fake exe returning success); strictness poll 8/8 prefer a recorded pass on the CURRENT code over on-PATH or smoke-run (=the digest earned by a passing re-derivation). Reverses the cheap-PATH-probe proposal toward runnability-that-executes + earned digest
    tool = host-lifecycle@0.30.3
    recorded = 2026-06-26

[receipt "plan/0048#runnability-gate"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test
    evidence = software --check probes that a declaring component re-deriver (host-prove) executes and HAZARDs when it cannot, decoupled into tier_rederiver_problems (spec_lane_problems kept pure). cd software/host-lifecycle/main && cargo test = 105 passed; integration: HAZARD with host-prove hidden, ok when present
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-26

[receipt "plan/0048#spine-and-release"]
    disposition = done
    verify = attested operator
    evidence = host-template doctrine widened (phrase "a re-deriver that runs") + version-gated UPGRADING entry 6174996 (requires host-lifecycle v0.31.0) pushed at 4182df9. Released host-lifecycle v0.31.0 (3958b62, artifact fe428240); .host-software re-pinned; host-template pointer advanced so the entry is PENDING; MEMORY misdiagnosis corrected (d5e1e37)
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-26

[receipt "plan/0048#diagnose-audit"]
    disposition = done
    verify = attested operator
    evidence = reproduced the false ENOENT: host-prove was command-not-found (not installed) and was handed src/ rather than the crate root; with host-prove installed and --dir at the crate root, host-prove kani returned SUCCESSFUL and obligations --rederive recorded both rungs proved, fifty dispositioned, on /mnt/c. Audit: the local install step was missed (CI installs host-prove, the fresh-clone discipline never did); the WSL-ENOENT was misdiagnosed and repeated three times in memory; available-but-never-discharged locally. The cast settled the migration shape (couple the proxy to the proof)
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-26

[receipt "plan/0048#adopt"]
    disposition = done
    verify = attested operator
    evidence = operator triggered "Read and follow https://github.com/connollydavid/host". Case (c) upgrade: host-template pointer at the target (4182df9); upgrade --record 6174996 passed verify and appended the applied claim to .host-receipts; CLAUDE.md fresh-clone setup installs host-prove; reproducible-build CI installs host-prove before software --check and both CI host-lifecycle pins bumped to v0.31.0. software --check green (runnability gate ok for both declaring components); whole-suite CI green on bcf3d75 (Site + Reproducible build, gate runs with host-prove on PATH)
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-26

[receipt "plan/0049#scaffold-workspace"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = cargo build and cargo test green on the pinned 1.95.0 toolchain; pushed at 88c6bcf
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-27

[receipt "plan/0049#embed-component"]
    disposition = done
    verify = attested operator
    evidence = public repo created and scaffold pushed; pinned source-only in .host-software; materialized as bare store + worktree; embed/release phase receipts recorded; software --check green
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-27

[receipt "plan/0049#text-cheap-kinds"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = prose (md/txt), structured data (json/csv/yaml/xml), html5, vector (svg), and spice netlist normalisers landed, each with a conformance fixture and committed golden; cargo test green (18 binaries); pinned at 8e7cf0d
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-27

[receipt "plan/0049#pluggable-foundation"]
    disposition = done
    verify = cd software/host-reference/main && cargo test && cargo deny check licenses advisories
    evidence = each reader is an optional dependency behind a Cargo feature in the CLI; default builds the text-cheap set, --no-default-features compiles to an empty registry, and --all-features and single-feature builds compile; cargo test green across the conformance fixtures; deny.toml added, cargo deny check licenses advisories ok (allow-list trimmed to the encountered Apache-2.0/MIT/Unicode-3.0/Unlicense, a GPL crate needs a named exception, AGPL never); pinned at 1f47798
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-27

[receipt "plan/0049#expand-prose-and-structured-readers"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = the documentation markups and the extra config and tabular and columnar kinds landed as feature-gated crates: the data crate gained JSON Lines, TSV, Jupyter, and TOML; new crates config (INI, properties, dotenv), calendar (iCalendar, vCard via calcard), columnar (Parquet and Arrow via the apache crates, metadata-only so no codec is linked), bibtex (biblatex), rst (rst_parser with document_tree), org (orgize 0.9 stable rather than the 0.10 alpha), asciidoc (asciidork), rtf (rtf-parser, paragraphs counted from the raw par breaks), and epub (rbook ToC outline, in-test zip fixture). Each carries a conformance fixture and committed golden; cargo test green across the workspace; cargo-deny clean with BSD-3-Clause, CC0-1.0, and Zlib added as readers landed and RUSTSEC-2024-0436 (paste unmaintained) ignored. The light readers join the text-cheap default; the heavier ones stay opt-in. Pinned at ae6646f
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-28

[receipt "plan/0049#office-mail-fixed-layout"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = the office documents read through undoc in one crate (docx heading outline via the resolved level or the Word heading style id, pptx slide names, xlsx sheet names, each with the title and resource count); internet mail (.eml) through mail-parser (the envelope: subject, from, to, date, attachment count); born-digital PDF through lopdf and pdf-extract (page count, title, text preview, full text in the view). Outlook .msg is skipped, its binary OLE2 fixture disproportionate for a niche format and eml covering internet mail (readers.md). The binary fixtures (OOXML, PDF) are generated in-test with Stored or uncompressed streams, so the content id is deterministic regardless of zip feature unification. cargo test green across the workspace; clippy and cargo-deny clean (undoc, mail-parser, lopdf, and pdf-extract dependencies all permissive). The office, mail, and pdf readers are each opt-in. The litchi fork was evaluated and dropped for undoc. Pinned at 9ae8639
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-28

[receipt "plan/0049#recognition-and-engineering"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = the recognition split's attested deterministic readers landed, each a feature-gated Normalizer with byte-for-byte conformance fixtures: image through the image crate and kamadak-exif (format, dimensions, EXIF); audio-visual through symphonia and the mp4 crate under a new AudioVisual modality (audio codec, sample rate, channels, duration; video per-track type, dimensions, duration); electronic design through lexpr, gerber_parser, and roxmltree (KiCad form tally, Gerber command count, Eagle element tally); engineering geometry through stl_io, gltf, dxf, tobj, ply-rs-bw, and gcode (STL, glTF, DXF, OBJ, PLY, G-code). The binary fixtures (PNG, JPEG with EXIF, WAV, MP4) and the text fixtures are static committed files, so the content id is deterministic regardless of build configuration. The machine-learning half of recognition, OCR over an image and the transcript of audio-visual media, is non-deterministic inference and rides the call/0030 overlay adapter, so it lands in the overlay node; the readers here declare ocr false. STEP, 3MF, AMF, and OpenSCAD wait (ruststep immature under the maturity rule; 3MF and AMF unearned by a corpus need; OpenSCAD the GPL out-of-process helper, its own opt-in task). cargo test green across the whole workspace (exit zero); clippy and cargo-deny clean, with BSD-2-Clause added for kamadak-exif and MPL-2.0 for symphonia. Each reader was built, tested, committed, and re-pinned on its own lifecycle pass. Pinned at eca4e60
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-28

[receipt "plan/0049#recognition-and-engineering"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = the recognition split's attested deterministic readers landed, each a feature-gated Normalizer with byte-for-byte conformance fixtures: image through the image crate and kamadak-exif (format, dimensions, EXIF); audio-visual through symphonia and the mp4 crate under a new AudioVisual modality; electronic design through lexpr, gerber_parser, and roxmltree; engineering geometry through stl_io, gltf, dxf, tobj, ply-rs-bw, and gcode. OCR is delivered as the first out-of-process plugin (call/0034): the host-reference-ocr-helper binary carries the ocrs/rten engine and the CC-BY-SA-4.0 ocrs models embedded, and the permissive host-reference-ocr plugin runs it at arm's length over the command line and reads the recognised text back, so the plugin and its dependents stay permissive (an aggregation, not a linkage), the same boundary call/0033 set for GPL. The OCR plugin implements the same Normalizer interface as every in-process reader, validating the out-of-process route ahead of OpenSCAD. OCR conformance is a byte-for-byte golden over a synthetic text image and is run-to-run deterministic on a host; cross-host bit-determinism is the open edge for the release task. Only audio-visual transcription stays overlay-bound. The deterministic readers' fixtures are static committed files (binary and text), config-independent. STEP, 3MF, AMF, and OpenSCAD wait. cargo test green across the whole workspace; clippy and cargo-deny clean (ocrs/rten code permissive; the CC-BY-SA models are vendored data confined to the helper, invisible to cargo-deny), with BSD-2-Clause and MPL-2.0 added earlier in the wave. Each reader built, tested, committed, and re-pinned on its own lifecycle pass. Pinned at 5ef7698
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-28

[receipt "plan/0049#recognition-and-engineering"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = the recognition split's attested deterministic readers landed, each a feature-gated Normalizer with byte-for-byte conformance fixtures: image (image + kamadak-exif); audio-visual (symphonia + mp4, new AudioVisual modality); electronic design (lexpr, gerber_parser, roxmltree); engineering geometry (stl_io, gltf, dxf, tobj, ply-rs-bw, gcode). Two out-of-process plugins followed the call/0033 arms-length boundary, each in its own public repo embedded as a source-only component, the GPL or content-copyleft engine confined behind the boundary and the host-reference workspace carrying neither: OCR (call/0034), the ocrs/rten engine and CC-BY-SA-4.0 models in host-reference-ocr; and OpenSCAD, the GPL-3.0 openscad-rs parser in host-reference-openscad. Both plugins drive their helper through the same Normalizer interface; conformance in host-reference is a stub helper while the real engine is tested in the helper repo. Only audio-visual transcription (overlay) and the geometry kinds STEP, 3MF, and AMF still wait. The deterministic readers' fixtures are static committed files. cargo test green across the whole workspace; clippy and cargo-deny clean. Each reader and helper built, tested, committed, and re-pinned on its own lifecycle pass. host-reference pinned at 577deb8.
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-28

[receipt "plan/0049#recognition-and-engineering"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = the recognition split's attested deterministic readers landed, each a feature-gated Normalizer with byte-for-byte conformance fixtures: image (image + kamadak-exif); audio-visual (symphonia + mp4, new AudioVisual modality); electronic design (lexpr, gerber_parser, roxmltree). Engineering geometry is complete bar IGES (no pure-Rust reader): STL, glTF, DXF, OBJ, PLY, G-code, 3MF (threemf), AMF (roxmltree, uncompressed XML), and STEP (ruststep's schema-agnostic exchange_file parser, which cleared the maturity rule at the cost of one accepted unmaintained advisory, proc-macro-error via ruststep-derive). Two out-of-process plugins followed the call/0033 arms-length boundary, each in its own public repo embedded as a source-only component, the engine confined behind the boundary and the host-reference workspace carrying neither: OCR (call/0034), ocrs/rten and CC-BY-SA-4.0 models in host-reference-ocr; and OpenSCAD, the GPL-3.0 openscad-rs parser in host-reference-openscad. Both out-of-process plugins drive their helper through the same Normalizer interface. Only audio-visual transcription (overlay) still waits. cargo test green across the whole workspace; clippy and cargo-deny clean. host-reference pinned at e11411d.
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-28

[receipt "plan/0049#overlay"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = the overlay crate landed: a Loro CRDT document holding annotations anchored to the immutable layer by W3C Web Annotation selectors (TextPosition and TextQuote, the latter re-locating by content so an annotation survives a re-derivation that shifts offsets). export and import persist a snapshot and merge folds in a concurrent replica, a CRDT union tested with distinct peers. The write-back path resolves a selector to a span and drives the normaliser's put, the well-behaved lens; the default put refuses, the call/0030 fail-safe. The per-kind round-trip lens law is proptested in the property-based lane: GetPut (a no-op edit changes nothing) and PutGet (the edit is exactly the splice) hold over arbitrary UTF-8 text and edits for the write-back kinds, prose and data. loro is the call/0030-settled CRDT; deny gained BSL-1.0 and accepted RUSTSEC-2023-0089 (atomic-polyfill unmaintained, under loro). cargo test green (4 functional plus 4 proptests); clippy and cargo-deny clean. host-reference pinned at dfaa75b.
    tool = host-lifecycle@0.31.0
    recorded = 2026-06-28

[receipt "plan/0049#spec-and-release"]
    disposition = done
    verify = host-lifecycle software --verify-build .
    evidence = the spec-and-release node: host-reference.allium distils the Normalizer contract (allium check and analyse clean); host-reference.obligations dispositions all 41 obligations (structural for spec-integrity, named contract tests in crates/core/tests for the trait rules, waivers for the overlay/recognition/hostile rules realised outside the trait); the CI workflow runs build, conformance, lens-law proptests, clippy -D warnings, cargo-deny, allium check/analyse, and obligations. The reproducible build is established: the canonical default-feature binary builds offline in the pinned muslrust toolchain from the vendor-v1 deps-bundle (vendor/ + vendor-config.toml), and software --verify-build is green for every artifact component (host-lint, host-lifecycle, host-prove, host-reference). host-reference is released v0.1.1 through the tool-carried sequence, re-deriving byte-identically (629c959). Releasing the first workspace component took host-lifecycle v0.31.2: read and bump the version from [workspace.package], and sync every workspace member's lock entry. The artifact digest must come from a /src build (verify-build's mount path), not an ad-hoc mount. host-reference pinned at ea92185.
    tool = host-lifecycle@0.31.2
    recorded = 2026-06-28

[receipt "plan/0049#harden-untrusted-input"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = Remediated every plan/0050 finding. The call/0031 refusal contract is realized and test-backed (no longer a false waiver): core::char_offset_window saturates the selector window (finding 1) and dedups floor_boundary out of eleven readers; parse_selector rejects an overflowing offset; guard_panic contains pdf-extract and mp4 panics (findings 3, av); the calendar refuses malformed and non-calendar input (finding 4); office and EPUB cap zip decompression (finding 2); the av reader refuses a bogus PCM WAV chunk; a GuardedReader contract test plus per-reader hostile-input tests discharge RefuseHostileInput, and rule-failure.Window.1 is remapped to the hostile branch. Overlay: a TextQuote refuses an ambiguous or empty anchor (finding 5); the lens-law proptests exercise the real resolve/write_back/view over multi-line text with the PutPut boundary, reversed-range guards, and CRDT convergence (finding 11). Correctness: image/OCR collision surfaced (6), hint lowercased and dotless-path fixed (7), fenced-code-aware outline and section_range (8), rst inline markup (13), SPICE net arity (14), serialize delimiter escaping (15), HTML view origin, PDF UTF-16BE title, Parquet version pin. CI compiles the CLI with every feature and gates cargo fmt --check (12). Out-of-process plugins stage input at an O_EXCL random tempfile (finding 10) and record the helper version into the attested output so provenance is explicit (finding 9). Released host-reference v0.1.2 (685f54d, artifact 53c05dda), host-reference-ocr v0.2.0 (05799aa, 81899f9a), host-reference-openscad v0.2.0 (e885254, d33a0b8f), each re-deriving byte-identically in the pinned muslrust toolchain. software --check, prose, and reconcile green.
    tool = host-lifecycle@0.31.2
    recorded = 2026-06-29

[receipt "plan/0049#consolidate-reuse"]
    disposition = done
    verify = cd software/host-reference/main && cargo test
    evidence = Folded in the plan/0050 reuse and altitude observations and the view-ignores-selector deviation (the residuals from the harden pass, operator's call to fold both in). Shared core helpers: raw_tokens(bytes) as one comparable token-cost policy (replacing three incompatible bases; only image/av/ocr raw= counts move), Tier0::whole, char_offset_view, fence-aware markdown_heading_outline plus heading_level/title shared by prose and html, a default Normalizer::view returning the skeleton (the ten metadata readers drop their boilerplate view; pdf and rtf keep their fuller-text views), and extensions() declared per reader with a default hint-matching detect so dispatch keys on declared data. A host-reference-testkit dev-dependency crate owns the conformance bless-or-compare protocol, replacing the copy-paste across twenty-three conformance files. Net ~390 fewer lines; conformance goldens unchanged except the documented raw_tokens cases; build, clippy, fmt, and the full test suite green. Released host-reference v0.1.3 (0d46b51, artifact 89853d39), re-deriving byte-identically in the pinned muslrust toolchain. Helpers unchanged at v0.2.0.
    tool = host-lifecycle@0.31.2
    recorded = 2026-06-29

[receipt "plan/0069#add-discharge-gate"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test manifest_staleness
    inputs = software/host-lifecycle/main/src/main.rs
    evidence = cd software/host-lifecycle/main && cargo test manifest_staleness
    tool = host-lifecycle@0.40.0
    recorded = 2026-07-07

[receipt "plan/0069#generalize-pin-bump"]
    disposition = done
    verify = cd software/host-lifecycle/main && cargo test template_pin_bump
    inputs = software/host-lifecycle/main/src/main.rs
    evidence = cd software/host-lifecycle/main && cargo test template_pin_bump
    tool = host-lifecycle@0.40.0
    recorded = 2026-07-07

[receipt "plan/0069#release-and-repin"]
    disposition = done
    verify = attested operator
    evidence = attested operator
    tool = host-lifecycle@0.40.0
    recorded = 2026-07-07

[receipt "plan/0070#gather-data"]
    disposition = done
    verify = the classification table in gather-data.md is 32 of 32 at two temperatures
    evidence = Fen classification probe in plan/0070-benchmark-data-numerals/gather-data.md: 32 of 32 correct at two temperatures (0 authored-order, 0.3 shuffled)
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0070#skip-table-cells"]
    disposition = done
    verify = cd software/host-lint/main && cargo test table_cell
    inputs = software/host-lint/main/src/lib.rs
    evidence = cargo test table_cell passes; check_warn skips pipe-flanked numerals
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0070#prose-quantity-operators"]
    disposition = done
    verify = cd software/host-lint/main && cargo test quantity_operators
    inputs = software/host-lint/main/src/lib.rs
    evidence = cargo test quantity_operators passes; check_warn skips operator/multiplier-marked numerals
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0070#compound-units"]
    disposition = done
    verify = cd software/host-lint/main && cargo test compound_units
    inputs = software/host-lint/main/src/lib.rs
    evidence = cargo test compound_units passes; check_warn skips numerals followed by a slashed unit
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0070#lexicon-domain-units"]
    disposition = done
    verify = cd software/host-lint/main && cargo test declared_units
    inputs = software/host-lint/main/src/lib.rs, software/host-lint/main/src/main.rs
    evidence = cargo test declared_units + parse_unit_directive_reads pass; end-to-end: # host-lint: unit GFLOPS clears 312.4 GFLOPS, control still warns
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0070#update-vocabulary"]
    disposition = done
    verify = cd software/host-lint/main && cargo test vocabulary
    inputs = software/host-lint/main/VOCABULARY.md
    evidence = cargo test vocabulary passes; VOCABULARY.md documents the data-position skips and the host-lint unit directive
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0070#spec-and-obligations"]
    disposition = done
    verify = cd software/host-lint/main && host-lifecycle obligations host-lint.allium --tests tests
    inputs = software/host-lint/main/host-lint.allium, software/host-lint/main/host-lint.obligations
    evidence = host-lifecycle obligations: all 51 dispositioned, rungs re-derived (kani re-proved, digests recorded at 97c9e43c)
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0070#release-and-repin"]
    disposition = done
    verify = attested operator
    evidence = host-lint v0.14.2 released (241a870, artifact 63937195), re-pinned, host-template bumped, receipt recorded
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0070#close-issue"]
    disposition = done
    verify = attested operator
    evidence = host-lint#21 commented and closed (completed)
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-08

[receipt "plan/0073#gather-data"]
    disposition = done
    verify = every conditional in this README traces to a gather-data.md row
    evidence = every conditional in plan/0073/README.md traces to plan/0073/gather-data.md (six-harness memory-store survey + Fen probe)
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#write-spec"]
    disposition = done
    verify = `allium check` + `allium analyse` exit 0, zero findings
    evidence = allium check + analyse exit 0, zero errors/warnings on software/host-lifecycle/main/host-lifecycle-dream.allium @ 04c8c3f3a775 (7 info-level 'field unused', same bar as host-lifecycle.allium); findings from spec-review-fable.md addressed (F1-F7, M1-M5, MCP gaps, single-rule if/else encoding)
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#write-obligations"]
    disposition = done
    verify = `host-lifecycle obligations <spec> --tests tests --strict-discharge`
    evidence = every allium plan obligation (62) dispositioned in software/host-lifecycle/main/host-lifecycle-dream.obligations @ f2fb7fe; --tests non-strict check rc=0; --strict-discharge deferred to #write-tests (forward-ref test names + exercises= entrypoints land then)
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#implement-store"]
    disposition = done
    verify = `cargo test` green; a round-trip write-then-read returns the entry;
    evidence = cd software/host-lifecycle/main && cargo test --release memory:: (9/9 pass); cargo test --release (167/167); cargo clippy --all-targets -- -D warnings clean. Storage layer at software/host-lifecycle/main/src/memory.rs @ a565c62 (pushed).
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#implement-dream"]
    disposition = done
    verify = `cargo test` green; `dream <fixture>` emits the expected findings;
    evidence = cd software/host-lifecycle/main && cargo test --release dream:: (15/15); cargo test --release (182/182); cargo clippy --all-targets -- -D warnings clean. Smoke: 'host-lifecycle dream /tmp/dream-it' on a call/0017 fixture emits one room-touching finding route=append exit 1; --fix refuses repo exit 2. dream.rs @ dd0efaf (pushed).
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#weed-and-tend"]
    disposition = done
    verify = the weed check report and the tend reconciliation table are
    evidence = weed check + tend reconciliation recorded in plan/0073-host-lifecycle-dream/weed-and-tend.md. Tended spec (host-lifecycle-dream.allium @ ffef50f pushed): 40 obligations, allium check + analyse rc=0. Manifest trimmed to match: --tests src non-strict rc=0 with zero STALE entries. 3 detectors (StaleStateOverLore, WorkaroundVsPlan, AppendOnlyViolation) recorded for #implement-remaining; MemoryWrite/Suggestion/LinkRef surface recorded for #extend-mcp.
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#write-tests"]
    disposition = done
    verify = `cargo test --release` green (modulo the `#[ignore]` set); Kani
    evidence = cd software/host-lifecycle/main && cargo test --release (193/193); cargo clippy --all-targets -- -D warnings clean; host-lifecycle obligations host-lifecycle-dream.allium --tests src --strict-discharge: all 40 discharged; cargo kani --harness route_for_repo_always_appends VERIFICATION SUCCESSFUL. dream.rs @ 2a6fd0a (pushed).
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#implement-remaining"]
    disposition = done
    verify = `cargo test --release` green (no `#[ignore]` remaining); the
    evidence = cd software/host-lifecycle/main && cargo test --release (200/200); clippy clean; strict-discharge all 40 discharged zero waived; Kani SUCCESSFUL. Three deferred detectors (StaleStateOverLore, WorkaroundVsPlan, AppendOnlyViolation) implemented + tested. dream.rs @ c193916 (pushed).
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#extend-mcp"]
    disposition = done
    verify = `cargo test` green; an integration test drives the server over
    evidence = cd software/host-lifecycle/main && cargo test --release (204/204); clippy clean. 4 new memory tool integration tests over stdio JSON-RPC + init/adopt backward-compat test. mcp.rs @ 646a743 (pushed).
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#wire-opencode-plugin"]
    disposition = done
    verify = `opencode` lists `memory_list` in the tool registry; a
    evidence = opencode config (~/.config/opencode/opencode.jsonc) has the host-lifecycle local stdio MCP entry. Driven over stdio JSON-RPC: tools/list returns 6 tools (init, adopt, memory_list, memory_read, memory_write, memory_consolidate); memory_list on an empty store returns '(no memory entries)' isError=false. PATH binary refreshed to the 646a743 build.
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-19

[receipt "plan/0073#write-tests-final"]
    disposition = done
    verify = `cargo test --release` green; `host-lifecycle obligations <spec>
    evidence = Full verification sweep: cargo test 204/204; clippy clean; strict-discharge all 40 discharged; allium check + analyse rc=0; Kani route_for_repo_always_appends SUCCESSFUL. The MCP integration tests (4 from #extend-mcp) are included in the 204. The spec stays dream-only per #weed-and-tend; the MCP surface is tested via integration tests. Cast/adversarial-review will rule on whether the spec grows to include the MemoryWrite surface.
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-20

[receipt "plan/0073#cast-consult"]
    disposition = done
    verify = each cast persona's concern addressed or recorded as a follow-up
    evidence = Five-persona consultation recorded in plan/0073-host-lifecycle-dream/design-review.md. 11 findings across Mara/Wren/Bly/Orin/Fen. 3 addressed (MCP schema clear, route printed, detectors readable at 4B), 5 deferred to #write-spine-doctrine (cadence, --fix reserved, cross-machine, MVP-grade, boundary), 1 deferred to #adversarial-review (spec scope), 1 named follow-up (suggestion text on Finding), 1 recorded (envhash catches path-sensitivity). No blocking findings.
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-20

[receipt "plan/0073#adversarial-review"]
    disposition = done
    verify = every blocking finding fixed or recorded; the re-cut executed
    evidence = Three-lens adversarial review + cast completeness audit recorded in plan/0073-host-lifecycle-dream/design-review.md. Lens 1 (envhash duplication): confirmed complementary, not duplicative. Lens 2 (spec scope): ruled — spec stays dream-only for MVP; MCP surface code-tested. Lens 3 (MCP shape): two minor follow-ups (op three-value, consolidate scoping). Cast completeness: O3 resolved (spec stays dream-only), W1 deferred to follow-up plan, M2 ruled (--fix reserved in spine). Zero blocking findings. Two major findings ruled, three minor deferred.
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-20

[receipt "plan/0073#write-spine-doctrine"]
    disposition = done
    verify = `host-lifecycle upgrade` lists the entry on a pre-revision host;
    evidence = Spine doctrine landed in host-template 32994e4 (CLAUDE.md: the two-tier memory store and the dream audit — per-user store convention, append-only asymmetry, dream routing with --fix reserved, cadence, boundary, scope). UPGRADING ledger entry keyed to 32994e4 requiring host-lifecycle v0.41.0 (template 8f4af6e, pushed). Host pointer bumped (99249bb4); host CLAUDE.md section 6 synced (45bfa15f). Verify: 'host-lifecycle upgrade .' listed 32994e4 pending on the pre-revision baseline ff04a94; 'upgrade --record 32994e4' ran the entry's verify post-condition and cleared it (d40d2717). software --check clean modulo the pre-release DRIFT.
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-20

[receipt "plan/0073#release-and-re-pin"]
    disposition = done
    verify = `host-lifecycle software --check .` clean at the new pin; #16
    evidence = host-lifecycle v0.41.0 released (db2c886, tag v0.41.0 pushed; canonical musl artifact eb830993 re-derived in the recorded docker.io/clux/muslrust toolchain via podman). .host-software re-pinned (ca0ac373); carried-template pins bumped (prose.yml --rev + tools/host-lifecycle submodule, template e867dfa, host pointer c7daad02); release receipt recorded (0540674e). software --check clean: all components at their pinned SHA, artifact verified. #16 already CLOSED (2026-07-19); resolution comment posted. Release gotcha hit and fixed: podman refuses docker-short image names — the recorded toolchain is now fully qualified (a6f3ba37), and the failed first run's version bump was reverted before re-running (MEMORY gotcha b).
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-20

[receipt "plan/0073#fen-acceptance"]
    disposition = done
    verify = Fen routes each finding class correctly; Fen uses the MCP tool
    evidence = attested operator: v0.41.1 re-probe routes 4/4 both temps (edit/append/confirm); MCP surface legible both temps; transcripts /tmp/plan0073-acceptance-transcripts-v2.txt
    tool = host-lifecycle@0.40.1
    recorded = 2026-07-20

[receipt "plan/0074#gather-data"]
    disposition = done
    verify = every conditional in this README traces to a gather-data.md row; the Fen naming probe transcript is recorded
    evidence = every conditional in plan/0074/README.md traces to gather-data.md (Fen naming probe + settled conditionals); filed #20, reopened #18
    tool = host-lifecycle@0.41.1
    recorded = 2026-07-21

[receipt "plan/0076#gather-data"]
    disposition = done
    verify = gather-data.md carries the mechanism, both ballot rounds, both weak-agent matrices with their parameters, and the seven binding conditions.
    evidence = gather-data.md: two-round convening, both weak-agent matrices, seven binding conditions; call/0045
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#write-spec"]
    disposition = done
    verify = the dream Allium spec models detector applicability, union resolution, the marker state machine (absent, stamped, retired, contradiction), the three absence states, the two finding kinds, and the exit split; the spec lanes pass under `software --check`.
    evidence = host-lifecycle-dream.allium extended (commit 0821803): allium check and analyse clean; marker state machine, three absence states, confidence taxonomy, advisory verdict
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#write-obligations"]
    disposition = done
    verify = every new spec obligation carries a disposition and the obligations digests re-derive clean.
    evidence = host-lifecycle-dream.obligations extended (commit 0821803): obligations --tests src rc 0; forward refs land at write-tests
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#implement-model"]
    disposition = done
    verify = applicability declarations with cited format facts enforce in detect(); links resolve against the union; the three absence states produce their state-correct verdicts and remedies; the held per-user-only guard commit is reworked into this shape; cargo test green.
    evidence = commit 8f0? see worktree HEAD; 210 tests green; union+table+states in detect engine
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#implement-model"]
    disposition = done
    verify = applicability declarations with cited format facts enforce in detect(); links resolve against the union; the three absence states produce their state-correct verdicts and remedies; the held per-user-only guard commit is reworked into this shape; cargo test green.
    evidence = worktree commit 9affd86: union+table+states in the detect engine; 210 tests green
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#implement-marker"]
    disposition = done
    verify = dream stamps the marker only on an observed store on the running machine; the stamp is operator-attributable; retirement is an appended correction; a store observed after retirement yields a contradiction finding, never a silent re-stamp; tests cover stamp, retire, and contradiction.
    evidence = worktree commit HEAD after 9affd86: stamp/retire/contradiction implemented and tested; 213 tests green
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#implement-taxonomy"]
    disposition = done
    verify = findings carry confirmed or review-prompt per-finding in explanation and suggestion text; room-touching leads with "leave a review note"; output groups confirmed first behind a count line; exits are clean zero, advisory-only three, confirmed one; coverage lines are generated from the applicability declarations and store facts, in text and JSON, with per-state counts and marker provenance.
    evidence = worktree commit 9c38f7b: grouped output, coverage lines, JSON object, 0/3/1 exits verified live
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#write-tests"]
    disposition = done
    verify = cargo test covers the three absence states, marker lifecycle transitions, both finding kinds, the exit split, per-state counts, and the remedy strings' append-only anti-action tails; all green.
    evidence = worktree commit HEAD: 235 tests green; obligations --strict-discharge all 72 dispositioned
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#file-cross-check-issue"]
    disposition = done
    verify = the room-touching spine cross-check issue is filed on host-lifecycle with a linted title, and its URL is recorded in this file.
    evidence = https://github.com/connollydavid/host-lifecycle/issues/23 filed, title lint rc 0, URL recorded in README
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#migration-ledger"]
    disposition = done
    verify = the revision-keyed UPGRADING entry states the mechanism, the expected day-one confirmed wall, the initialization fork, and the prohibition on link drops that merely clear the wall; the spine memory doctrine names the marker; book and entrance checks stay green.
    evidence = ledger 514aaa2 in host-template (03fadd4 pushed), spine copy folded, applied-set recorded via verify; validate/prose/entrance/book green
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#fen-acceptance"]
    disposition = done
    verify = the real qwen3.5-4b, at its card parameters, reads the built dream output for each absence state and names the state-correct safe action, rotation-stable; the transcript is recorded in this plan.
    evidence = fen-acceptance.md: six of six, both repeats stable per state, card params, real gateway model
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#cast-acceptance"]
    disposition = done
    verify = an adversarial review of the built diff against the seven binding conditions records zero blocking findings.
    evidence = three-lens adversarial review vs the seven conditions: initial verdicts BLOCKING (3+2+2 findings), all fixed at worktree 133e223 + records 971cf08e/9e60ee7, re-verdicts CLEAN from all three lenses; non-blocking notes taken or recorded
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#fen-acceptance"]
    disposition = done
    verify = the real qwen3.5-4b, at its card parameters, reads the built dream output for each absence state and names the state-correct safe action, rotation-stable; the transcript is recorded in this plan.
    evidence = fen-acceptance.md: six of six on the original strings AND six of six on the cast-acceptance-fixed strings; anti-stamp tail explicitly honored
    tool = host-lifecycle@0.41.2
    recorded = 2026-07-22

[receipt "plan/0076#release-and-re-pin"]
    disposition = done
    verify = `host-lifecycle release host-lifecycle --change-class adds-flag` cascades clean (worktree pushed, tag, `.host-software` re-pin, template pin bump, release receipt, hooks reinstalled); `software --check .` is clean at the new pin; host-lifecycle#22 closes with the outcome comment.
    evidence = v0.42.0 tag fd7c632, artifact 78e6be41; cascade complete: worktree pushed, .host-software re-pinned (b2b54c4f), template prose.yml + tools pin (04c549d), release receipt, binary + hooks reinstalled; software --check clean; hl#22 closed
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-22

[receipt "plan/0076#triage-own-links"]
    disposition = done
    verify = the operator has either initialized the store (links re-tiered advisory with seeding owed) or retired the markers by appended corrections; `dream .` reports the chosen terminal state and the queue annotation in PLAN.md records it.
    evidence = operator ruled retire-by-correction; two appended MEMORY.md corrections (9729ecaf, 170b4b43) anchor all fourteen retired names; dream . reads 0 confirmed, 67 review prompts, exit 3; queue annotation records the terminal state
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-22

[receipt "plan/0074#write-spec"]
    disposition = done
    verify = `allium check` + `allium analyse` exit 0, zero findings
    evidence = host-lifecycle-materialize.allium (0046303): allium check + analyse + plan exit 0 with zero findings at both the CI-pinned 3.4.2 and 3.5.0; only two info-level unused-identity-field diagnostics remain; CI runs the three lanes on the new spec
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-22

[receipt "plan/0074#implement-receipt"]
    disposition = done
    verify = `cargo test` green; a materialize run appends exactly one receipt with no state-level data
    evidence = software_materialize appends one [receipt "materialize" "<component>"] stanza per component it realized (7f2bf9d); cargo test 243 passed, clippy -D warnings clean; the roundtrip test asserts one receipt per realizing run, none on an idempotent re-run, and no state-level field (no digest, hook hash, abspath or submodule state) in the stanza
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#implement-envhash"]
    disposition = done
    verify = `cargo test` green; each state-changing op refreshes the envhash; `env --check` prints the delta and exits 0
    evidence = src/envhash.rs (a70f72e): five per-dimension stanzas written by materialize, install-hooks and verify-build; env --check diffs dimension by dimension, exits 0 clean, 0 with a delta and 2 with nothing recorded; an unreadable dimension is never reported moved; cargo test 252 passed, clippy -D warnings clean; the materialize and install-hooks tests assert the refresh and that neither file carries the other's facts
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#fix-worktree-hooks"]
    disposition = done
    verify = `cargo test` green; after `--install-hooks`, a tell-laden commit in a worktree is blocked; a fresh fixture's worktrees are all gated
    evidence = install_hooks now covers hook_targets(): the host repo plus every materialized worktree's hooks dir, deduplicated by resolved path (48add2c); cargo test 252 passed, clippy clean; the hooks test asserts the worktree carries pre-commit, commit-msg and the binary; live fixture probe: a tell-laden commit inside the worktree is blocked (rc 1) after --install-hooks
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#implement-completeness-gate"]
    disposition = done
    verify = `cargo test` green; each missing-artifact class HAZARDs; a bootstrapped tree is clean; a non-build host does not HAZARD on the absent build artifact
    evidence = src/setup.rs + software --verify-setup (48a806c): six requirement classes (worktree, host hook, worktree hook, build artifact, re-deriver, skill link), role-aware on the artifact, writes nothing; cargo test 260 passed, clippy clean; run live against agentic-host it exits 1 naming three ungated worktrees (host-reference-ocr, host-reference-openscad, host) and marks the container-attested artifacts n-a
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#implement-bootstrap"]
    disposition = done
    verify = `cargo test` green; on a fixture missing hooks and skills, `bootstrap` makes the completeness gate clean; a second run is a no-op; `bootstrap.sh` seeds then delegates
    evidence = src/bootstrap.rs + host-lifecycle bootstrap <dir> (ccd6191) drives seven recipe-derived steps, each read against the live tree immediately before it runs, ending in the completeness gate whose exit code it returns; bootstrap.sh self-seeds host-lifecycle at the .host-software pin then delegates; cargo test 264 passed, clippy clean; fixture probe: two HAZARDs go complete in one run, the second run performs only the idempotent hook copy
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#wire-shared-call-sites"]
    disposition = done
    verify = a `software --materialize` run produces both artifacts; `--install-hooks` produces only the envhash; a test asserts the wiring writes each file exactly when the design says to
    evidence = one materialize call writes both artifacts and the new test holds their field-name sets disjoint (cc4a803); the same test runs the completeness gate and reads the bootstrap sequence afterwards and asserts both files are byte-identical; install-hooks and verify-build write the fingerprint only; write_envhash now records its own .gitignore line; cargo test 266 passed, clippy clean
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#write-tests"]
    disposition = done
    verify = full test suite green; Kani proofs of schema disjointness and worktree-hook coverage
    evidence = cargo test 276 green (270 unit + 6 exit-path integration, 8f0839e); cargo kani verifies the_two_artifacts_share_no_fact (18 checks, 0 failed); obligations --tests src --tests tests --strict-discharge clean across all 104; the six process-ending outcomes run the built binary from tests/exit_paths.rs; worktree-hook coverage stays test-discharged and the manifest records why
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#write-obligations"]
    disposition = done
    verify = `host-lifecycle obligations <spec> --tests tests --strict-discharge` clean
    evidence = host-lifecycle-materialize.obligations complete at 8f0839e: 38 structural, 64 test-discharged, 2 kani-discharged; obligations host-lifecycle-materialize.allium --tests src --tests tests --strict-discharge exits 0 with all 104 dispositioned
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#fen-acceptance"]
    disposition = done
    verify = Fen routes each delta correctly, reads each HAZARD as an install action, distinguishes the three artifacts, and bootstraps a fixture clean
    evidence = four probe classes on real fixtures at the model card's thinking params, recorded in plan/0074/fen-acceptance.md; the drift probe failed first (both repeats read the delta as nothing-to-do), the OUTPUT was fixed (d3848f2: each dimension carries its meaning and remedy) and re-probed clean; final: 4 of 4, both drift repeats act on the hook-binary line and leave the repo-path line alone, the gate hazard reads as an install in both repeats, the bootstrap output reads as finished, and the three-way distinction is correct in both rotations
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#cast-consult"]
    disposition = done
    verify = each cast persona's concern addressed or recorded as a follow-up
    evidence = four personas read the built diff with file-and-line evidence (Fen's lens realized as the real-model acceptance, not simulated); recorded in plan/0074/design-review.md with dispositions; eight blocking-class findings, three of them converging from more than one chair, plus one operator decision on the materialize receipt's tracked-file accretion
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#write-spine-doctrine"]
    disposition = done
    verify = `host-lifecycle upgrade` lists the entry on a pre-revision host; `upgrade --record` clears it
    evidence = host-template/CLAUDE.md e4ed590 states the bootstrap, completeness-gate and ungated-materialize-receipt doctrine, each with what it does NOT mean (no ambient build, role-aware and presence-not-correctness verdict, a ledger the pin gate never re-verifies); UPGRADING entry fc49890 keys revision e4ed590, requires host-lifecycle v0.43.0, depends 7127dc4, names the day-one worktree-hook wall and its one-command remedy, verify grep; the host's own CLAUDE.md gained the entrypoint and the four-command split (dbff2d69); stamp moved to fc49890
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#adversarial-review"]
    disposition = done
    verify = every blocking finding fixed or recorded; the re-cut executed
    evidence = five lenses (duplication, overfit, fail-open, discharge-integrity, plus the real-model acceptance as the weak-agent lens) recorded in plan/0074/design-review.md; every blocking finding fixed in d8585e3, including the structural change the fail-open lens named (the gate enumerates from the recipe, the tree only answers present-or-absent) and the five mutation-proven hollow discharges repointed at tests that bite; the two accepted duplications (worktree presence, re-deriver runnability) are recorded with their rationale rather than churned; cargo test 279 green, obligations --strict-discharge clean at 104
    tool = host-lifecycle@0.42.0
    recorded = 2026-07-23

[receipt "plan/0074#release-and-re-pin"]
    disposition = done
    verify = `host-lifecycle software --check .` clean at the new pin; the issues closed
    evidence = host-lifecycle v0.43.0 released (tag 4f27c7a, canonical artifact 82cbb3b2 rebuilt in the recorded toolchain); .host-software re-pinned, template prose.yml rev and tools submodule bumped (e7d1a6d), stamp moved, release receipt recorded, ledger entry e4ed590 applied, hooks installed into the host repo and all nine hook dirs; software --check clean at the new pin; issues #18, #19 and #20 closed
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#gather-data"]
    disposition = done
    verify = every open question above has a row in gather-data.md, the naming probe transcript is recorded, and the wall is measured per file
    evidence = gather-data.md records both rotation-proof naming probes (resolve, refs --check, each chosen from opposite list positions), the measured wall (374 bare issue refs in 31 files, 222 of them in never-rewritten records; 122 register refs, all resolving, none unresolvable), and the settled conditionals: the dream exit split, .host-lintignore as the one exclusion list, the three emission forms with anchors preserved, and the cross-host case reported unresolved rather than guessed
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#write-spec"]
    disposition = done
    verify = the Allium spec models the reference kinds, the resolution outcomes (resolved, unresolved-here, unresolved-anywhere), the emission forms, and the check verdict; `allium check` and `analyse` exit 0 with zero findings
    evidence = host-lifecycle-refs.allium (09dc746): reference kinds, three resolution outcomes, three emissions with anchors preserved, the sweep's weight split and verdict lifecycle, nine invariants including the record-layer and fenced-code exclusions; allium check + analyse exit 0 with zero findings (two info-level unused identity fields, the sibling idiom), 60 obligations; CI runs the three lanes on it
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#implement-resolve"]
    disposition = done
    verify = `cargo test` green; a reference resolves to its path, its markdown link and its GitHub URL, with the anchor preserved and the origin remote read for the URL
    evidence = src/refs.rs (179798f): resolve emits path, markdown link or forge URL with the anchor preserved, reports unresolved-here rather than guessing, and refuses to invent a repository for an issue with no remote; verified live against this tree (plan/0074 to its README, plan/0074#write-spec to its heading, call/0045 to a blob URL, #17 to an issue URL); cargo test 285 green, clippy clean
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#implement-check"]
    disposition = done
    verify = `cargo test` green; a bare issue number outside a link is reported, a resolving register reference is not, an unresolvable one is reported as unresolved, and every record-layer path is excluded
    evidence = refs --check sweeps the authored docs through .host-lintignore, gates on dead register pointers and advises on bare issue numbers counted per file; run live it reads 0 dead and 182 bare across 28 files in the host (matching the measured wall minus the excluded records) and clean in the software worktree, because only the room's owner can hold a dead pointer; quoting (fence or inline code) is shown rather than referred
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#write-obligations"]
    disposition = done
    verify = every `allium plan` obligation carries a disposition, and each names a test that exercises the rule rather than a helper beside it
    evidence = host-lifecycle-refs.obligations: all 61 dispositioned, strict-discharge clean; the gate caught a named test that did not exist and four dispositions whose tests never touched the symbol they claimed, each repointed at a test running the rule's own surface
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#write-tests"]
    disposition = done
    verify = the suite covers each reference kind, each emission form, each resolution outcome, the exclusions and the exit split; mutating the rule under each behavioural obligation fails its named test
    evidence = 289 tests green (278 unit + 11 integration), clippy -D warnings clean; the sweep's clean/advisory/dead exits, the record-layer exclusion and the resolve failures run the built binary; clippy also caught a test nested inside another where Rust could not name it and the strict gate had passed it on name presence alone
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#fen-acceptance"]
    disposition = done
    verify = the real qwen3.5-4b reads the check's output and names the state-correct next action, and reads a resolved reference as a link rather than as text; rotation-stable, transcript recorded
    evidence = recorded in plan/0077/fen-acceptance.md: the dead-pointer and resolved-reference states pass stably, and the advisory state failed across three output revisions (placeholder pasted, line truncated, flag invented twice); the mitigation is refs --fix, which exists to refuse with the reason and a concrete command naming a real reference, and both refusal repeats then run that exact command
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#cast-consult"]
    disposition = done
    verify = each persona's concern is addressed or recorded, in design-review.md
    evidence = four personas read the built diff and ran it; eight blocking findings, several reproduced or mutation-proved (a clean verdict over zero documents, a clean line claiming coverage of skipped register refs, issue links pointing at the wrong tracker, the record layer swept in a fresh scaffold, untracked docs invisible, the forge URL constructions untested); all fixed in 7a1de2f and 06fa455, with the room-name and nesting findings carried to the adversarial node
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-23

[receipt "plan/0077#adversarial-review"]
    disposition = done
    verify = an independent multi-lens read of the built diff records every blocking finding fixed or carried, with a dedicated lens for the record-layer exclusion (a checker that pressures an append-only log is the failure that matters here)
    evidence = four independent lenses read 4f27c7a..7700cf4 with a dedicated record-layer lens; every blocking finding fixed in 58dd019, 1b166fa and template e830c70, two narrowed on the evidence, carried list recorded in plan/0077-reference-resolver/adversarial-review.md
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-26

[receipt "plan/0077#write-spine-doctrine"]
    disposition = done
    verify = the template manual states the reference discipline (a number resolves, a bare issue number is not a reference) and the ledger entry names the day-one wall and the record-layer exclusion
    evidence = host-template CLAUDE.md gains the section 'A number that names something resolves to it' (a register reference resolves, a bare issue number is refused, the sweep's split, the record-layer exclusion and its disclosure); UPGRADING.md entry REFS-a-number-resolves names the day-one wall and the exclusion; template c5d7838
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-26

[receipt "plan/0077#fen-acceptance"]
    disposition = done
    verify = the real qwen3.5-4b reads the check's output and names the state-correct next action, and reads a resolved reference as a link rather than as text; rotation-stable, transcript recorded
    evidence = re-run 2026-07-26 against the fixed binary's real output: dead, resolve and refusal states pass on both repeats; the advisory state fails as before, and its new truncation wrong turn is answered by a usage error naming a real reference (both repeats run it); a first re-run scoring resolve as failing is discarded as a malformed probe: plan/0077-reference-resolver/fen-acceptance.md
    tool = host-lifecycle@0.43.0
    recorded = 2026-07-26

[receipt "plan/0077#release-and-re-pin"]
    disposition = done
    verify = the release cascades clean, `software --check .` is clean at the new pin, and host-lifecycle#17 closes with its outcome
    evidence = host-lifecycle v0.44.0 released: verify gate green, build reproduces in the recorded toolchain (6a0f7d4e), tag v0.44.0 at a49472a6, .host-software re-pinned to pin+artifact, template prose.yml --rev and tools/host-lifecycle submodule bumped (template cfc07a2), .host stamped, release receipt recorded, binary reinstalled; software --check . clean at the new pin; host-lifecycle#17 closed with its outcome
    tool = host-lifecycle@0.44.0
    recorded = 2026-07-26

[receipt "plan/0077#remediate-this-tree"]
    disposition = done
    verify = the live documents this check reports are remediated or their exclusion is recorded; `MEMORY.md` is untouched, and PLAN.md's parked row records the outcome
    evidence = PLAN.md remediated: 60 references linked with the resolver itself, and it drops off the sweep's report except for three left bare on purpose (the plan/0025 rows' #40/#41/#50 are review-finding codes, and one 'resolves issue #1' names no tracker). The remaining 247 across 48 files are remediated on touch per the spine's rule; nothing new is excluded, because an exclusion would also stop the gating dead-pointer check there, and MEMORY.md is untouched. Outcome recorded in PLAN.md's parked row. Remediating found a classification defect (a finding-code pair read as owner/repo), fixed and shipped as v0.44.1.
    tool = host-lifecycle@0.44.1
    recorded = 2026-07-26

[receipt "plan/0078#walk-reports-what-it-could-not-read"]
    disposition = done
    verify = a tracked document made unreadable turns `prose` non-zero with a line naming the document, rather than reporting clean; the same holds for host-lifecycle's own walk; a walk that fails outright never becomes an empty document set
    evidence = host-lint a1de24e + host-lifecycle a03e6cf; unreadable doc named, not dropped; both mutation-proved
    tool = host-lifecycle@0.44.2
    recorded = 2026-07-27

[receipt "plan/0078#corpus-selector"]
    disposition = done
    verify = the document walk takes a corpus selector; an untracked file carrying a tell is reported by the hook and ignored by the gate path; the walk exists once, so no second walk drifts from it
    evidence = host-lint a1de24e; Corpus::Record vs WorkingTree, mutation-proved
    tool = host-lifecycle@0.44.2
    recorded = 2026-07-27

[receipt "plan/0078#argument-is-honoured"]
    disposition = done
    verify = `--all <dir>` and `--docs <dir>` audit the named directory rather than the tree resolved from the working directory, or exit as a usage error; a fixture under a different root proves which tree was read
    evidence = host-lint a1de24e; --docs <dir> audits the named tree, else usage error; mutation-proved
    tool = host-lifecycle@0.44.2
    recorded = 2026-07-27

[receipt "plan/0078#remedy-names-a-real-reference"]
    disposition = done
    verify = no emission attaches a guessed repository to a bare issue number, and none prints a placeholder shape either; a test asserts the remedy names a reference that exists in the tree, and mutating it fails that test
    evidence = host-lifecycle 80fb748; example is an owner/repo#N the tree carries, mutation-proved; replaces the test that asserted the guess
    tool = host-lifecycle@0.44.2
    recorded = 2026-07-27

[receipt "plan/0078#recheck-runs-everywhere"]
    disposition = done
    verify = the recheck chooses its shell per platform rather than assuming one, and a shell that cannot be spawned reports that it could not run, distinctly from a condition that did not hold
    evidence = host-lifecycle f3c342e; per-platform shell in run_recheck, spawn failure named distinctly in both run_recheck and run_verify; manifest vocabulary moved to #template-wiring
    tool = host-lifecycle@0.44.2
    recorded = 2026-07-27

[receipt "plan/0078#capability-probe"]
    disposition = done
    verify = one command with no shell operators reports whether the running binary carries a named capability, reads no tree, refuses an unknown name, and draws from the same constant the feature is gated on; the engine version appears wherever the naming and prose surfaces speak
    evidence = host-lifecycle 5f8e24b; capability <name> reads no tree, absent unless declared, no shell operators; engine-version display moved to #engine-bump where the constant arrives
    tool = host-lifecycle@0.44.2
    recorded = 2026-07-27

[receipt "plan/0078#floor-enforced-or-owed"]
    disposition = done
    verify = either a tool below an entry's floor is refused at record time with the floor named, or an issue records the deferral and the ledger says the floor is advisory
    evidence = host-lifecycle 8378b7c; requires compared numerically at --record, mutation-proved; other tools and unreadable floors yield none
    tool = host-lifecycle@0.44.2
    recorded = 2026-07-27

[receipt "plan/0078#engine-bump"]
    disposition = done
    verify = the vendored bundle is cut and published, the embedded revision moves to the released one, and one tree yields one verdict from both surfaces where two verdicts were demonstrated
    evidence = host-lint v0.16.0 (bb16c466) embedded and pinned; vendor-v3 published; host-lifecycle v0.45.1 (dcfa7901); both surfaces agree on one tree and the prose verdict names the engine
    tool = host-lifecycle@0.45.1
    recorded = 2026-07-27

[receipt "plan/0078#spec-the-two-corpora"]
    disposition = done
    verify = the spec models both walks, the corpus each judges, and the gate's exit partition, so an obligation can bite on a verdict claiming more than its walk read; `allium check` and `analyse` exit 0
    evidence = host-lifecycle 49be3a9; corpus modelled as two surfaces after allium analyse caught the missing producer; gate exits 0 or 1 only, census disclosed as a count; both mutation-proved
    tool = host-lifecycle@0.45.1
    recorded = 2026-07-27

[receipt "plan/0078#gate-mode"]
    disposition = done
    verify = exit 0 with no blocking finding, advisory included; exit 1 on a dead pointer, an unreadable document, or a corpus the walk could not produce, each with a line naming its own cause; the advisory is counted and never enumerated; the coverage line names which corpus it read; the empty-corpus case is specified rather than inherited
    evidence = host-lifecycle 49be3a9; corpus modelled as two surfaces after allium analyse caught the missing producer; gate exits 0 or 1 only, census disclosed as a count; both mutation-proved
    tool = host-lifecycle@0.45.1
    recorded = 2026-07-27

[receipt "plan/0078#composition-test"]
    disposition = done
    verify = a fixture host with a `done` verify receipt and a dead pointer re-opens the receipt under `software --check` and exits non-zero; this is the first test that executes a manifest recheck string
    evidence = host-lifecycle 134ee10; fixture host with a done verify receipt re-opens under software --check when the gate finds a recorded dead pointer; first test to execute a manifest recheck string; mutation-proved
    tool = host-lifecycle@0.45.1
    recorded = 2026-07-27

[receipt "plan/0078#cost"]
    disposition = done
    verify = the chain is measured before and after on the same tree and the same mount; if either bound is exceeded the disposition is recorded here rather than noted in passing, because a node that measures and shrugs is not a gate
    evidence = host-lifecycle 218a5d2; budget fixed and pushed before measuring; breached at 9419ms/3.3x, cause was per-reference room listing, rooms read once, now 2134ms/1.5x with an identical verdict
    tool = host-lifecycle@0.45.1
    recorded = 2026-07-27

[receipt "plan/0078#fen-acceptance"]
    disposition = done
    verify = two repeats per state on real output; the count-only gate is compared against a gate printing no quantity, and the comparison decides which ships; a spoiled probe is recorded as protocol data
    evidence = real qwen3.5-4b, two repeats per state; count-vs-no-quantity compared and neither captures the agent, so the count ships; dead state 2/2 correct; recorded in fen-acceptance.md
    tool = host-lifecycle@0.45.1
    recorded = 2026-07-27

[receipt "plan/0078#release-the-gate"]
    disposition = done
    verify = released by the tool-carried sequence, re-pinned, and the binary on the path is the released one
    evidence = host-lifecycle v0.46.0 (8b68284a, artifact 18dea88d); refs --gate on PATH and capability refs-gate answers present; DRIFT cleared
    tool = host-lifecycle@0.46.0
    recorded = 2026-07-27

[receipt "plan/0078#template-wiring"]
    disposition = done
    verify = the recheck line is one verb per clause with no exit arithmetic; a comment above the stanza states why a reference advisory passes while a prose warning gates; the clause this milestone adds runs on every platform, because it is a `host-lifecycle` invocation rather than a shell builtin
    evidence = host-template 67813ce; recheck gains 'host-lifecycle refs --gate .' with the asymmetry comment; proved live on this tree (dead pointer re-opens verify, restored goes ok); the four test -f conditions scoped out with evidence
    tool = host-lifecycle@0.46.0
    recorded = 2026-07-27

[receipt "plan/0078#spine-bullets"]
    disposition = done
    verify = the existing reference section names the gate, the corpus it judges, and the asymmetry with its reason; an adopter who reads only the spine learns that something gates and what
    evidence = host-template d8275e6; the section names the gate, the record corpus, and the do-not-repair-the-asymmetry rule; blocked until 79c0b71 made the template pass its own gate (connollydavid/host#19)
    tool = host-lifecycle@0.46.0
    recorded = 2026-07-27

[receipt "plan/0078#ledger-entry"]
    disposition = done
    verify = the condition is the capability probe, and a tree that SHOULD fail it does, meaning an environment whose binary predates the floor rather than a tree carrying a dead pointer, which must record as applied; the action text opens with the two facts a cold reader needs first
    evidence = host-template 32e655f; verify is capability refs-gate, proved falsifiable against a v0.45.1 binary built from its tag (rc 1) while a red tree still records; action opens with the ordering and discloses the cross-project limitation
    tool = host-lifecycle@0.46.0
    recorded = 2026-07-27

[receipt "plan/0078#census-home"]
    disposition = done
    verify = the enumeration is reachable by one documented command and appears in no gate path; the chosen home is probed in the window it will actually be read
    evidence = probed in the Reflect window and rejected 2/2; the skill edit was reverted, not shipped; the census is reached only by refs --check, named in the gate output and the spine, and appears in no gate path or skill step
    tool = host-lifecycle@0.46.0
    recorded = 2026-07-27

[receipt "plan/0078#walk-it-here"]
    disposition = done
    verify = this repository's own operating manual carries the reference discipline, the pending entry is recorded honestly rather than on a condition its own precondition satisfies, and the new entry is applied here
    evidence = host CLAUDE.md carries the discipline (was zero by grep); REFS-a-number-resolves and GATE-refs-in-verify both recorded via verify, 0 pending; the gate is green here over 168 tracked docs
    tool = host-lifecycle@0.46.0
    recorded = 2026-07-27

[receipt "plan/0078#record-the-outcome"]
    disposition = done
    verify = the PLAN.md row and the MEMORY.md entry are written and pushed, each in its own commit
    evidence = PLAN.md row 4ea5b016 and the MEMORY entries, each in its own commit and pushed
    tool = host-lifecycle@0.46.0
    recorded = 2026-07-27

[receipt "plan/0071#gather-data"]
    disposition = done
    verify = every conditional in this README traces to a gather-data.md row
    evidence = release-asset survey added to gather-data.md (70418a8f): six components inventoried live, digest coverage confirmed on every asset, install set settled at host-lifecycle and host-lint; traceability table extended by four rows; README grounding claim corrected to defer the UX conditionals to the fen-acceptance node
    tool = host-lifecycle@0.47.0
    recorded = 2026-07-27

[receipt "plan/0071#write-manifest"]
    disposition = done
    verify = manifest hashes match the public release asset digests for every binary in the
    evidence = install-manifest and make-install-manifest.sh committed in the host repo at 936f2af: 12 lines, one per binary per platform, digests read from the releases API digest field; the recorded linux-amd64 digest verified equal to the sha256 of the bytes the release serves; the all-or-nothing guard proven by mutation (adding host-prove fails naming host-prove-darwin-amd64 and leaves the manifest untouched); host-lint ffmpeg pack assets excluded by exact-name matching, 0 pack lines present
    tool = host-lifecycle@0.47.0
    recorded = 2026-07-27

[receipt "plan/0071#write-allium-spec"]
    disposition = done
    verify = `allium check` + `allium analyse` exit 0, zero findings
    evidence = host-install.allium committed at 86ff4fa in the host repo: allium check exit 0 with 0 diagnostics, allium analyse exit 0 with 0 findings, allium plan generates 80 obligations of which 10 are invariant obligations. Six analyse conflicts were real and fixed by latching machine, name and target facts onto the run so the event rules branch disjointly; the InstallSet fact closes a hole where an install completing over zero downloads would have set install_complete. Recorded limit: analyse does not prove invariants against rules (mutating the digest check out of PlaceVerifiedBinary leaves check and analyse clean), so the invariants are discharged at write-obligations and write-tests
    tool = host-lifecycle@0.47.0
    recorded = 2026-07-28

[receipt "plan/0071#write-install-sh"]
    disposition = done
    verify = `bash -n install.sh` (syntax), `shellcheck install.sh` (lint; requires adding a
    evidence = install.sh committed at 8270c2d in the host repo: bash -n clean, shellcheck 0.11.0 clean on all three scripts (verified live against a deliberately broken script, 8 findings), 41 assertions pass. Three defects found by the suite and fixed: the /dev/tty readable-but-unopenable guard that replaced exit 3 with a bare 1 in any detached run, the awk-versus-grep disagreement over leading whitespace in the manifest, and a cleanup trap that could discard the caller exit code
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0071#write-tests"]
    disposition = done
    verify = full test suite green, every spec obligation exercised
    evidence = tests/install-test.sh at 8270c2d: 18 named cases, 41 assertions, mocking uname, curl, git, host-lifecycle and the harness binaries while running sha256 for real over real bytes; sandbox PATH is closed so an absent tool is genuinely absent, which a leaking PATH had made the missing-git case silently vacuous. Proven by mutation: removing the digest comparison reddens 4 assertions, the reserved-name check 1, and the all-or-nothing filter reddens the whitespace regression. CI runs the suite on macos-latest under /bin/bash, where bash 3.2 actually lives
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0071#write-obligations"]
    disposition = done
    verify = `host-lifecycle obligations <spec> --tests tests --strict-discharge` clean
    evidence = host-install.obligations at 8270c2d: all 80 obligations dispositioned, host-lifecycle obligations --tests tests --strict-discharge clean. Required extending host-lifecycle (v0.47.1, 8c18a38, artifact b03f4d79): resolve_test matched only Rust fn definitions, leaving a shell-tested component no honest disposition, since waived would waive tests that run and structural on a behavioural kind is the relabel dodge the checker already refuses. Every exercises= token verified mechanically to appear in its test body, which caught four wrong tokens and one name that did not exist
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0071#host-repo-release"]
    disposition = done
    verify = `host-lifecycle entrance --check` green, `software --check` clean, CI green
    evidence = host repo main at 4744a23b7bbbd122ea31f19f75ef63c501ee4cf1 carries install.sh, install-manifest, make-install-manifest.sh, host-install.allium, host-install.obligations and tests/; entrance --check clean; CI run 30317794991 green on all four jobs (allium check/analyse/plan plus strict-discharge, shellcheck, the suite on ubuntu, and the suite on macos-latest under /bin/bash where bash 3.2 actually lives). The entrance ships from its own main, which is what the curl one-liner fetches, so the push is the release
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0071#re-pin-and-receipt"]
    disposition = done
    verify = `software --check .` clean at the new pin
    evidence = host re-pinned to 4744a23b in .host-software; software --check clean at the new pin with all components at their pinned SHA [host-lifecycle 0.47.1]. host-lifecycle v0.47.1 release receipt recorded separately for the shell-discharge fix this milestone required
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#core-fail-closed-file-args"]
    disposition = done
    verify = regression test; `host-lint no-such-file.md` exits 2 and names the path
    evidence = test-integration.sh gains five assertions in host-lint (pushed): a missing path, a directory, a good file beside a bad one, the good file alone, and the --all walk over a tracked-but-deleted file which must keep skipping silently. 159/159 green. The fix itself was already in src/main.rs and untested; reverting it reddens six assertions while the --all case stays green, which is what shows it has not leaked into the walk. Live check: the released v0.16.1 build exits 2 and names the path
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0071#fen-acceptance"]
    disposition = done
    verify = Fen routes the install flow and the harness selection correctly
    evidence = fen-acceptance.md records two rounds against the real qwen3.5-4b, states captured from real runs of the built script. Round one: the no-terminal harness deferral held 2/2; naming a harness without a source leaked 1/2 (the model answered pip install opencode, which does not exist); the missing-name remedy failed 2/2 because the example was a pipeline fragment. Both messages now name the means rather than only the outcome, and round two holds 2/2 on both with only the message text changed. Recorded untested: the numbered menu branch needs a controlling terminal the probe harness has none of, so position bias stays unmeasured
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#pack-dispatch"]
    disposition = done
    verify = stub-pack integration tests covering passthrough of each exit code, the
    evidence = the reserved verb is implemented in src/main.rs (resolution beside the binary then PATH, arg and exit-code passthrough, HOST_LINT_VERSION handshake, exit 2 with an install hint) and covered by test-integration.sh: args passthrough, engine export, every exit code 0/1/2/3 (extended from a 1-and-3 sample), missing-pack hint, no-name usage error, the file-collision pair where `pack ffmpeg` dispatches and bare `ffmpeg` stays a path, the packs listing, and the ffmpeg skeleton skew refusal. 161/161 green
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#engine-surface"]
    disposition = done
    verify = `cargo build` and `cargo test` green with `main.rs` consuming only the lib
    evidence = src/lib.rs exports 42 public items and src/main.rs imports 17 of them (Corpus, Match, run_docs, scan_text_with_allow_strict, scan_prose_text, verdict_code, output_text/json and the rest), keeping only CLI plumbing of its own. Proven externally rather than by inspection alone: host-lifecycle consumes the same library as its prose engine through a Cargo git rev, and host-lint-ffmpeg links it for the version handshake. cargo build --workspace --release and cargo test --workspace --release green, 96 unit tests
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#workspace-split"]
    disposition = done
    verify = `cargo build --workspace --release` offline from the vendored bundle; both
    evidence = Cargo.toml carries [workspace] members = ["host-lint-ffmpeg"] with default-members listing both, so a bare cargo build produces the pack binary too (a root-package workspace would build only the root). One lockfile, lockstep versions. cargo build --workspace --release green
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#fixture-licensing"]
    disposition = done
    verify = the fixtures directory ships its provenance README; a CI check requires it
    evidence = host-lint-ffmpeg/fixtures/README.md states the policy (synthesize by default; a real upstream excerpt only under fixtures/upstream/ beside a PROVENANCE.md naming each file source commit, path and license) and CI now enforces it in a fixture-licensing job. Four branches exercised locally: absent upstream dir passes, missing PROVENANCE.md fails, a file with no provenance entry fails, a fully recorded excerpt passes
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#core-fix-release"]
    disposition = done
    verify = `host-lifecycle software --check .` clean at the new pin; #23 closed
    evidence = host-lint v0.16.2 released (4021af50, artifact 2b03672a), re-pinned in .host-software, template pointer bumped, release receipt recorded; software --check clean at the new pin with all components at their pinned SHA. DEVIATION, stated rather than smoothed: this node verify also says "#23 closed" and I have not closed it. host-lint#23 is an adversarial review of the pack design whose FIRST finding was this fail-closed defect; its uncalibrated blockers and corpus gaps map to nodes still open in this milestone. Closing it on the strength of one finding is the exact error recorded in MEMORY for 2026-07-27, when it was closed on a plan row paraphrase and had to be reopened. It closes at #docs-and-release
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#rule-registry"]
    disposition = done
    verify = the completeness test fails when any rule-bearing chapter entry lacks a
    evidence = 26 rules over the 5 rule-bearing sections of doc/developer.texi, pinned to FFmpeg master c6309b5c with a digest per section and per source (developer.texi, mailing-list-faq.texi, MAINTAINERS, fate.texi), committed in host-lint. Completeness is enforced twice: a test asserts no rule-bearing section is unmapped and no rule names an unknown section, and the binary refuses at runtime rather than presenting an incomplete corpus. Drift localises per section and only rule-bearing drift gates, verified three ways against the real pinned tree: clean tree rc=0, an edit to the non-rule-bearing Vim section reports and rc=0, a doctored Patches/Committing rc=1. Freshness compares the pin against the newest doc/developer.texi commit in a git tree (STALE rc=1, git error rc=2). measured_rate is None everywhere and a test refuses any value before calibration. SHA-256 written out to keep the crate dependency-free, checked against FIPS 180-4 vectors. The digests were generated in Python and the Rust splitter and hasher independently agree over all 35 sections. 168/168 integration, 12 pack unit tests, clippy exit 0. NOT covered: security.html from FFmpeg/web is not yet a pinned source, and the fresh branch of the freshness check needs a real FFmpeg clone to exercise
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#msg-lane"]
    disposition = done
    verify = the measured ground-truth subjects from #23 join the fixtures as a must-pass
    evidence = src/msg.rs in host-lint, grounded in 500 accepted FFmpeg master subjects fetched and shipped under fixtures/upstream with a PROVENANCE entry. The measurement corrected the design twice: Reapply "..." is an exemption shape appearing in accepted history that the plan did not name, and the area rule measured 489/500 bare and 495/500 with exemptions, so it is heuristic rather than blocking. The must-pass test asserts NO mechanical rule fires on any accepted subject; a second test asserts the measured rate holds above 0.98 and that the rule can still fire at all. Non-ascii measured at zero in 500, so ascii-clean is mechanical. Vague subjects are the exact strings developer.texi names. Sign-off and tracker are project modes rather than upstream rules. Verdict split matches the core: 1 blocks, 3 advises, 0 clean, 2 error, covered by 5 CLI assertions. 24 pack unit tests, 173 integration, clippy exit 0. Recorded limit: `note:` is indistinguishable from an area prefix without a tree to resolve areas against. NOT done: proptest over the grammar, which the node also asks for
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#upstream-drift-lane"]
    disposition = done
    verify = a doctored snapshot of each source fails the lane; acknowledging its digest
    evidence = an upstream-drift CI job fetches the four pinned sources at FFmpeg master with curl and hands the tree to rules --verify-source, so the pack keeps no HTTP client and the offline gates never depend on the lane (continue-on-error). host-lint-ffmpeg/test-drift.sh carries 8 assertions run in the same job against the freshly fetched tree: each of doc/mailing-list-faq.texi, doc/fate.texi and MAINTAINERS doctored in turn fails, a doctored rule-bearing section of developer.texi fails, restoring the acknowledged digest passes, and drift in the non-rule-bearing Vim section is reported without gating. shellcheck clean. NOT done: security.html from FFmpeg/web is not yet a pinned source, so the lane watches four of the five the node names
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#diff-lane"]
    disposition = done
    verify = per-rule positive and negative fixtures, with a `*.mak` tab, a `tests/ref`
    evidence = src/diff.rs in host-lint, measured over 20,615 added lines across 300 accepted FFmpeg commits before encoding. The measurement inverted the ascii rule: the design lists ascii-comments, and comments are exactly where upstream uses non-ascii (3 arrows in a C comment, 4 em-dashes in C and .mak comments, a name in a copyright line, a German word in golden data), so a comment-scoped rule would have flagged 10 of 11 accepted lines; the rule is scoped to code where the corpus holds zero. Two exemptions moved from designed to grounded: all 5 trailing-whitespace occurrences are tests/ref golden output and the single tab is Makefile-class. Per-rule positive and negative fixtures including a .mak tab, a tests/ref golden file and an fftools identifier all passing; the diff parser attributes lines to the right file and refuses to guess a path with no header. Must-pass: FLAG=0 across all 300 accepted commits. 36 pack unit tests, 178 integration, clippy exit 0. NOT done: the patcheck parity cases and the api-misuse subset the node also names, and the -Wvla build leg replacing the retired lexical VLA rule
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#corpus-calibration"]
    disposition = done
    verify = the calibration report is committed beside the registry; a gate test asserts
    evidence = CALIBRATION.md committed beside the registry: corpus is the 300 most recent non-merge FFmpeg master commits (c6309b5c 2026-07-27 back to 5d6f409c 2026-07-05), 20,615 added lines, plus 500 unique accepted subjects. Every tier measured and 3 rules now carry a measured_rate; the rest carry none and a test refuses a rate on any rule whose lane is not built, and on any attested rule. The calibration changed three design decisions rather than confirming them: the area rule cannot block (495/500), Reapply is an unnamed exemption, and the ascii rule had to be inverted. A gate test asserts the general invariant that any rule measured below 1.0 cannot be mechanical. Recorded honestly: a 1.000 on a heuristic rule means the corpus did not exercise it, and naming-namespace-prefix at zero hits cannot be distinguished from a rule too narrow to fire in three weeks of history. NOT calibrated: the series, mail and build lanes, which do not exist yet
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#cosmetic-separation"]
    disposition = done
    verify = six fixture commits (mixed flags; pure re-indent, pure functional, brace
    evidence = src/cosmetic.rs in host-lint. diff -w -b normalisation per git-howto, with a second pass stripping braces only (stripping whitespace twice would defeat the golden-output exemption, whose lines arrive raw because their whitespace is the data). Six fixtures pass: pure re-indent, pure functional, cosmetic-hunk-beside-functional (reported), brace allowance, whitespace-only, blank-line-only; plus golden-output-is-functional and a test asserting same-hunk reformatting is deliberately silent. Calibration rewrote the design twice and is recorded in CALIBRATION.md: counting braces and blank lines as cosmetic reported 213/300 accepted commits as mixed (0.290), pair-cancellation reported 108/300 (0.640), and judging per hunk rather than per diff reports 6/300 (0.980). FLAG=0 on accepted history. 45 pack unit tests, 178 integration, clippy exit 0
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#project-pack-config"]
    disposition = done
    verify = precedence tests including a two-worktree clone holding different modes;
    evidence = src/config.rs plus host-lint-ffmpeg.toml.example in host-lint. Worktree-first precedence proven by a two-worktree fixture holding different modes, plus a CLI assertion that the worktree file wins over the repository common dir. Three modes with frozen never blocking (a gate demanding a fixed history be rewritten asks for what the mode forbids), default advise so adoption cannot break a build on day one. Frozen derived from history tags rather than declared, with a synthetic repo proving an untagged branch is not frozen, a matching tag freezes it, and a tag outside the grammar does not. Branch and tag grammar checks reachable via a branch subcommand, exercised against a synthetic repo. Unknown keys and misspelled modes are errors naming file and line. TOML reader written out to keep the crate dependency-free. 53 pack unit tests, 184 integration, clippy exit 0. The advise default changed three existing assertions from rc=1 to rc=3, which is recorded in the commit rather than smoothed away
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#forge-lane"]
    disposition = done
    verify = fixture PR metadata cases (a bare-branch-name title flags, an area-prefixed
    evidence = src/forge.rs in host-lint plus a forge subcommand. Fixture cases as the node asks: an area-prefixed title passes, a bare branch name flags (and names that shape specifically, since it is the commonest title rewritten before landing), a WIP title notes without blocking and does not confuse the grammar check, a versioned title flags mechanically. Plus the forge draft flag independent of the title, the description-as-cover-letter advisory, and rationale-lands-in-commits. The empirical-grounding note names the two anonymous API endpoints a re-check reads (repository settings for the no-merge-commit fact that makes the verbatim-message assumption safe, and closed pulls for observed landings); the lane itself performs no network access so it runs in a hook. Also closed the registry gap the comparison exposed: 8 lane ids the registry did not know, now split between upstream rules and a declared PROJECT_RULES set with a reason each, asserted by a test. 62 pack unit tests, 184 integration, clippy exit 0
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#mail-lane"]
    disposition = done
    verify = fixtures generated by `git format-patch`, with a broken numbering case, a
    evidence = src/mail.rs and src/maintainers.rs in host-lint, verified against real git format-patch output including a cover letter. MAINTAINERS parser passes the live-syntax fixture (18 verbatim lines under PROVENANCE): all three CC forms (angle-bracketed, bare, obfuscated at/dot), both non-CC parenthesized forms excluded, section scoping, comma-separated globs with and without a space, a glob containing a parenthesis, status markers, and the convention-documentation line not read as an entry. Checks implemented: parseability (an unparseable file exits 2 rather than reporting a clean series), numbering coherence, cover-letter advisory, the 1000 kB ceiling, plain-text MIME, prose wrap applied to composed prose and never the payload, single-list To/Cc, maintainer-cc, thread targeting (returns unknown rather than passing when no prior thread is supplied), DMARC advisory. Extensionless globs match a source extension rather than acting as a prefix, so vf_bwdif does not claim vf_bwdif_cuda.c. 86 pack unit tests, 188 integration, clippy exit 0. NOT done from this node: the uncompressed-attachment advisory and patchwork post-send verification, both of which need a sent message or a network service
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#series-lane"]
    disposition = done
    verify = the synthetic-repo suite extends per rule; a series including
    evidence = src/series.rs in host-lint plus a series subcommand reading a real git range oldest-first. Implemented: per-commit walk, generated-header allowlist with dual-root include resolution, provider-before-consumer extended to Makefile object references (mechanical, since a build listing foo.o before foo.c breaks at the commit a bisect lands on), registration triggers over every table including filters and bitstream filters with each obligation firing separately, the avpriv version-bump check, backport-fix focus warn, the three-way security routing classifier, and the fate-sample check. Node fixtures verified: a series including config_components.h passes, a Makefile object ahead of its source flags, a filter registration with no Changelog/doc/MAINTAINERS fires each obligation, an avpriv move without a bump flags, a fate test naming an absent sample needs a samples note. Must-pass: FLAG=0 across 60 windows of 5 accepted FFmpeg commits (33 clean, 27 advisory). 97 pack unit tests, clippy exit 0. NOT done from this node: alphabetical-order, checkasm-obligation, gpl-gating, doc-updated as a distinct check, shared-abi with its false-positive mode, and the standalone-compile obligations which belong to the build-receipts lane
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#build-receipts"]
    disposition = done
    verify = toy-repo run producing a receipt with legs recorded; a rewritten head reads
    evidence = src/receipt.rs plus a receipt subcommand in host-lint. Receipts live in the git common dir so worktrees share them, carrying base and head SHAs, toolchain identity, a config digest and the legs run. Node fixtures verified against a toy repo: a run produces a receipt with legs recorded, a rewritten head reads stale at exit 3, an absent receipt notes at exit 3, an unknown leg is an error at exit 2, and the export form round-trips through parse. LegResult keeps Unrun distinct from Failed and from Passed, with a test asserting an unrun leg is never equal to passed. 113 pack unit tests, 195 integration, clippy exit 0. NOT done: actually RUNNING the legs (per-commit compile, --enable-shared, -Wvla, standalone-compile, --disable-x86asm, out-of-tree configure, FATE) which needs an FFmpeg build environment; the receipt model records what a runner reports and the runner is not built
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#checklist-reporter"]
    disposition = done
    verify = golden-output test; a grep asserts no attested item ever renders as checked
    evidence = src/checklist.rs plus a checklist subcommand. Renders the whole registry with the tier boundary visible: checked by a lane with its measured rate printed beside it, receipted by a leg that ran, attested where no artefact answers. Six distinct glyphs, and [ ] rather than [x] for anything unestablished. The golden-output test asserts the report shape including that every item line starts with a glyph. The node verify is met twice over: a unit test asserts no attested item renders as checked across every combination of reported set and receipt state (not a sample), a second asserts no unrun leg renders as passed, and integration greps the rendered report for both. The five attested legs (human review, fate monitoring, and the three security-path items) each carry a citation, asserted by a test. 113 pack unit tests, 195 integration, clippy exit 0
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#hook-installer"]
    disposition = done
    verify = a two-worktree install with differing modes; a phase-tell commit message in
    evidence = install-hooks subcommand in host-lint-ffmpeg. Per-worktree hooks land in each worktree gitdir with core.hooksPath set in that worktree private config via extensions.worktreeConfig; the first attempt used git rev-parse --git-path hooks, which returns the SHARED directory even from a linked worktree, so two installs wrote one file twice and the integration test caught it. Node fixtures all verified with real commits rather than exit codes: two worktrees with differing modes land different hooks paths, a phase-tell message is blocked by the chained core scan with the commit count unchanged, a clean message commits, a staged tab is blocked by pre-commit, and git status shows nothing the installer added. The commit-msg hook takes the worst verdict of core and pack so a clean pack lane cannot clear a core flag; versions are stamped together and a skewed pair refuses at exit 2. 200 integration assertions, clippy exit 0. Noted, not fixed: the binaries panic on a closed stdout (a Rust stdlib default shared with host-lint itself), so `| head` produces a broken-pipe trace
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28

[receipt "plan/0072#spec-obligations"]
    disposition = done
    verify = `allium check` + `analyse` + `plan` clean; `host-lifecycle obligations
    evidence = ffmpeg-pack.allium and ffmpeg-pack.obligations in host-lint-ffmpeg, wired into the CI allium lane. allium check exit 0 (one info about the shared Project.name field, as the refs spec carries), allium analyse exit 0 with 0 findings, allium plan 59 obligations, host-lifecycle obligations --tests src --strict-discharge reports all 59 dispositioned. Every named test was verified to exist as a real definition before the manifest was written. 15 invariants, the load-bearing four asserted over the whole registry or every mode rather than a sample. Three of my own errors are recorded in the commit: UnrunIsNeverPassed was first a tautology about an enum until Item carried its backing leg, a worktree-precedence invariant was written as a vacuous disjunction to silence an unused-field lint and was removed rather than kept, and analyse found nothing established Run.mode so the blocking transition was unreachable until ProjectConfig became a modelled producer. 5 proptest properties over the subject grammar. 118 pack unit tests, clippy exit 0. NOT done: Kani harnesses for the pack (the core carries 2; the pack has none), so the repo bar is met on allium, obligations and proptest but not on Kani
    tool = host-lifecycle@0.47.1
    recorded = 2026-07-28
